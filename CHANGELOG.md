# 变更记录

版本号按 [语义化版本](https://semver.org/lang/zh-CN/)。每条尽量写清**核实到什么程度**，
而不只是「做了什么」—— 这个项目要替人操作真实招聘账号，「已实现」和「已核实」之间的
差别就是用户会不会被坑。

## 0.2.0（2026-08-10）

覆盖方案 004–012。主线是两件事：**投递侧从零到两阶段闸门**，
以及**届别推导从字符串猜测改成站点自己的分派依据**。

### 新增

**M6 飞书代投（方案 010）** —— `jobagent/submitters/feishu.py`

- 四租户共用一套飞书前端（蔚来 / 小鹏 / 字节 / 商汤），采集层一份代码覆盖
- **两阶段提交闸门**：`prepare()` 填表、截图、发一次性 `confirm_token` 后**停在提交按钮前**；
  `execute(confirm_token)` 才点提交。没有 `--yes` 开关，这是故意的
- **回读摘要**：`execute()` 把页面上**现在**的值读回来重算摘要，和你确认过的对不上就拒绝提交。
  读和写走同一条寻址 —— 否则对不上是虚警，对上了更糟（拿一张没动过的卡替被改过的卡背书）
- **歧义守卫**：可见命中不是 1 个就抛，不用 `.first` 挑。多条目段落靠
  `scope` + `scope_index` 收窄到具体某张卡片
- **隐私政策勾选**放在 `execute()` 而不是填表阶段 —— 那是一次同意动作，不是一个字段。
  勾之前核三道：可见命中数为 1、框旁文案里有「隐私政策」、勾完回读状态真的变了
- 敏感字段（身份证 / 手机号）展示与入库打码；下拉值不做近似匹配
  （「硕士研究生」不自动变「硕士」，那等于代投替人改了申报内容）
- 归因区分「岗位已关闭」/「SPA 渲染层 404」/「登录门」/「中间态转圈」四种停下的理由

**判据体检（`cli checkup`）**

- 逐条核投递器里靠字符串认页面的常量：中文字段名、CSS-modules 类名前缀、
  勾选框旁文案、下拉选项全称、提交按钮文案
- 只读，不填不投；有一条红就退出码 1
- 存在的理由：这些判据坏掉的方式全是**静默**的 —— 命中 0 个，然后代投交出一张
  几乎空的表单加一句「填了 2 个字段」

**M7 投递记录出口（方案 012）** —— `cli applications`，只读的投递漏斗与拦截原因

**飞书届别第三通道（方案 011）** —— `job_subject` 招聘项目名参与届别推导

### 变更

- **腾讯届别推导改按 `projectId` 分桶**（方案 009，替代 007 的标签字符串匹配）。
  发现过程值得记一笔：007 落地后去核观察项，才发现站点自己是按 `projectId` 分派届别的
  （`renderProjectMeta`），而我是按 `recruitLabelName` 字符串重建的 —— 猜对过一阵子不等于判据对
- **换季刷新**（方案 008）：改了届别常量，存量岗位跟着刷新，不留 441 行过期数据；
  届别过期时 CLI 能报出来
- **事务边界收拢到 `ingest.sync()`**（方案 005）
- **届别补全 + 高优 bug 修复**（方案 004 + 006）
- 测试 41 → 504 条

### 修复

- **`.gitignore` 漏掉真正在用的登录态目录**。忽略规则写的是 `.browser-profile/`，
  而命令行实际传的 `--user-data-dir` 是 `.browser` —— 160MB 招聘账号 cookie 一直处于
  未忽略状态。改成通配 `.browser*/`：目录名由命令行决定，判据就不该写死一个
- `ARRAY_CARD` 类名前缀末尾少写两个下划线会把 `apply-form-array-card-content__xxx`
  一起命中（实测 1 → 11）

### 已知边界

**`execute()` 最后那一下「提交」从未真跑过。** 所以 `_is_success` / `_is_duplicate` /
`_is_job_closed` 的成功文案仍是推测的 —— 只验过「未提交时不误报」。

- 折叠段（工作经历 / 项目经历 / 证书等）**目前一个都不填**。填写机制已在真页面探通，
  缺的是数据：`profile.yaml` 里 `internships` / `projects` 还是空列表
- 实习经历段被默认勾上的「没有实习经历」关掉。要填得先取消勾选，而那是改申报内容，
  必须先上确认清单
- 投递器测试全跑在假页面上。真页面的判据靠 `checkup` 在线核，不靠单测

## 0.1.0

M1–M5 发现侧 MVP：数据采集（适配器模式）、指纹增量 diff、YAML 画像、
规则引擎匹配打分、Rich CLI。

