# 文档结构

三层，**职责不混用**。改东西之前先想清楚该动哪一层。

| 层 | 文件 | 职责 | 硬要求 |
|---|---|---|---|
| **方案文档** | [SPEC.md](SPEC.md) | 当前已确认的产品与技术事实 | **必须与实现一致**。每条能被 §9 的命令验证 |
| **Wiki** | [WIKI.md](WIKI.md) | 需求背景、决策及原因、替代方案、调研、实验、复盘、版本变化 | **不作为实现依据**。已证伪的条目**标注保留，不删** |
| **知识库** | [kb/](kb/README.md) | 只放允许 Agent 检索引用的业务事实 | 每份带来源/版本/生效时间/权限范围/更新负责人；**未审核的 Wiki 内容不许进** |

## 什么变了该更新哪一层

| 发生了什么 | 更新哪里 |
|---|---|
| 方案或实现变化 | **优先** `SPEC.md`（再看要不要连带改 kb）|
| 做了个决策、跑了个实验 | `WIKI.md` |
| Agent 能用的业务事实变了 | `kb/` 对应文件，**并涨版本号** |
| 要改代码 | **先写方案** `plans/NNN-*.md`（见根目录 `CLAUDE.md`）|

## 为什么 Wiki 和知识库对「错了的结论」处置相反

Wiki 里躺着七八条已证伪但保留原文的结论（「飞书没有校招」「校招接口找不到」…）。
留着是对的：删了就没法验证后来的结论是不是编的。
但 **Agent 检索到它们就会照着错的答**，所以知识库里的错条目是**删除**，
同时在 `WIKI.md §3.2` 记一行「曾经写的 / 实际 / 错在哪」。

## 方案（按顺序走的实现记录）

`plans/` 下按编号，模板 [_TEMPLATE.md](plans/_TEMPLATE.md)：

| 编号 | 内容 | 状态 |
|---|---|---|
| [001](plans/001-代投人工闸门.md) | 代投人工闸门（回填的样例）| 已复盘 |
| [002](plans/002-飞书招聘采集.md) | 飞书招聘采集 | 已复盘（**§3/§7/§8 三条结论已被 003 证伪，原处已标注**）|
| [003](plans/003-校招门户采集.md) | 校招门户采集（`website-path`）| 已复盘（校招 8594 条落核对库；顺带修了 `apply_url` 全 404）|
| [004](plans/004-届别补全.md) | 届别补全（标题作第二条观测通道 + 按维度放宽）| 已核实（严格档 41 → **95**）|
| [005](plans/005-事务边界收拢.md) | 事务边界收拢到 `ingest.sync()`（记账走侧连接）| 已完成（8 条新测试，7 个变异全部被捕获）|
| [006](plans/006-高优bug修复.md) | 高优 bug 修复（空 diff 事件、`recruit_type` 兜底等）| 已复盘 |
| [007](plans/007-届别窗口区间.md) | 腾讯届别改按招聘标签推导（应届=入口届别，在读实习=不限）| **已被 009 替代**（2026-08-09，标签匹配 → projectId 分桶）|
| [008](plans/008-届别换季刷新与过期告警.md) | 换季刷新存量届别 + 届别过期告警（反馈报告问题 5）| **刷新已落地**（2026-08-09，真库 441 行）；**告警仍草稿** |
| [009](plans/009-按projectId分桶.md) | 腾讯届别改按 projectId 分桶（而不是标签字符串）| 已落地（2026-08-09。逻辑等价重构，真库 0 行改动）|

007 和 008 有**硬顺序**，两步都已经走完（2026-08-09）：007 改届别推导，而存量岗位
不会跟着变（`grad_year` 不在指纹里，正是 008 要修的）。实测那一刻真库 807 行里有
804 行会被 `sync` 静默跳过，只有 1 行因为别的字段也变了而搭上便车 —— 那 1 行最坏，
它让人以为生效了。所以 007 之后跑了 008 的刷新：

```bash
uv run python -m jobagent.cli refresh-grad-year --source tencent_join           # 预演
uv run python -m jobagent.cli refresh-grad-year --source tencent_join --apply    # 真写
```

换季改 `CURRENT_CAMPUS_YEAR` 之后同样要跑这一步，否则会看到「改了代码、
`sync` 说 `updated=0`、库里没动」。

**刷新 441 行之后，SPEC §9 的三档条数 95/1952/2533 一个没动。** 不是刷新没生效 ——
真画像同时收 26 和 27，实习岗从「27 命中」变成「不限命中」，一进一出抵平了。
这个 bug 只在画像收窄到单届时才看得见：`[26]` 10→41、`[28]` 0→17。

两份方案都在 2026-08-09 对 join.qq.com 做过核实，004 §8 那条「要动 `GRAD_WINDOW`
得先去门户上看页面写的是哪一届」的前置条件已满足：**应届的 26 是对的**（站点写
「2026应届生招聘」），**实习的 27 是错的**（站点写「不限毕业时间」）。核实同时推翻了
007 的第一版方案和 008 的第一版告警判据，两份文档的 §11 都记着为什么。

## 其余文件

| 文件 | 是什么 | 归属层 |
|---|---|---|
| [ATS_RESEARCH.md](ATS_RESEARCH.md) | 「按公司还是按 ATS 分层」的调研，含「七次口径错」 | Wiki 性质 |
| [M6_MANUAL_TEST.md](M6_MANUAL_TEST.md) | 投递层手工测试步骤 | SPEC 附属 |
| [M6_SUMMARY.md](M6_SUMMARY.md) | M6 实现总结 | Wiki 性质（历史）|
| `../产品架构文档.md`、`../校招Agent-MVP方案计划书(1).md` | 早期文档，**写在 ATS 分层之前** | Wiki 性质（历史），与当前实现有出入，以 `SPEC.md` 为准 |
