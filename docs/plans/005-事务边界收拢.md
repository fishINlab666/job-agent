# 方案：事务边界收拢到 ingest.sync()

> 编号 `005` · 日期 `2026-08` · 状态：**已完成**
> 涉及文件：`jobagent/ingest.py`、`jobagent/db.py`、`tests/test_ingest.py`
> 验证重跑：2026-08-09，三条实测结论全部复现，另发现一处方案漏项（§0 偏差 8）
> 落地：2026-08-09，8 条新测试 + 7 个变异全部被捕获（§9），其中一条测试的原判据是假的、已重写

---

## 0. 当前进度

| 步骤 | 状态 | 核实命令 / 实际偏差 |
|---|---|---|
| ~~方案三方验证~~ | ~~已过期~~ → **已重跑（2026-08-09）** | 重跑的是**实测三条结论**，不是再找三个 subagent 读一遍文档（理由见下面「重跑口径」）。三条全部复现，另外发现一处方案漏项，见偏差 8 |
| 过一遍方案，把 §3 的假设逐条核实 | 已核实（行号已校准 2026-08-09） | 只有 4 处传参，**全在 `ingest.py` 自己**，仓库里没有别的调用方。位置现在是 `ingest.py:94 :96 :115 :134`（原写 `:79 :81 :100 :119`，004/006 落地后推移） |
| 独立连接会不会抢写锁（§3 原来列为待验假设） | **已核实 ×3 版本** | 最小复现：主连接持写事务时侧连接写 `runs` → `OperationalError: database is locked`。sqlite 3.50.4 / 3.53.1 / **3.51.0（2026-08-09 补）** 三个版本一致。三条硬顺序的报错逐条对上（含约束 1 的两种报错） |
| 四条路径的落盘结果 | **已核实（2026-08-09 重测）** | 副本上重新实现，四条路径各起一条新连接只读已提交状态，结果与 §5 末尾那张表**逐格一致** |
| 收拢后 `test_dry_run_*` 还全绿吗（§3 原来假设「全绿」） | **已核实（2026-08-09 重测）** | `side = conn`：**4 failed / 367 passed**，失败的四条测试名与方案记的完全一致，泄漏状态也是 `sources=1` + `runs=1`。加 `_NoCommit` 后 **371 passed**。基线 371（方案里写的 351 是 004/006 之前的数） |
| **§5 骨架的 `...` 占位掩盖了一次真实重构** | **新发现（2026-08-09）** | 照骨架字面实现（`try` 只包 `fetch()`），**重复 external_id 这条路径 run 停在 `running` 且 error 为空** —— 正是本方案要修的那个 bug 只修了一半。必须把整个写库循环收进同一个 `try`。见偏差 8 |
| **代码实现** | **已完成（2026-08-09）** | `db.py` 去掉 3 个 `commit=` 形参并改写三段 docstring；`ingest.py` 加 `_NoCommit`、侧连接、整个函数体收进一个 `try`。两处缺陷都按副本上验证过的形状落地：`PRAGMA database_list` 取库文件（不打真库）、`side.close()` 放 `finally`、成功路径 `finish_run` 放 `else` |
| **测试通过** | **已核实** | `uv run pytest -q` → **379 passed**（落地前基线 371，新增 8 条：`test_ingest.py` 加 `TestTransactionBoundary` 类）。§10 原计划 6 条，实际写了 8 条 —— 多出的两条是反向对照，理由见 §10 |
| **这些测试真的在测东西吗** | **已核实（7 个变异全被捕获）** | 逐条变异见 §9。**一条测试的原判据是假的**：`test_repeated_syncs_do_not_leak_connections` 原来数存活的 `sqlite3.Connection` 对象，而 `side.close()` 只写在 `else` 里（失败路径漏关）时它**照样全绿** —— CPython 引用计数把没人引用的连接立刻回收了。改成盯 `db.connect` 开出来的对象是否 closed 才抓得住 |
| 真库有没有被碰到 | 已核实 | 落地前后 `data/jobagent.db` 只读查：`runs` 仍是 7 条、最新一条 `2026-08-06T17:09:27`、`jobs` 9401 条、`sources` 6 个 —— 没有测试数据写进去。变异测试全在 `/tmp/mut005` 副本上跑（副本不含 `data/`、`profile.yaml`） |

**重跑口径（2026-08-09）。** 「三方验证」在本仓的原意是三个视角交叉核方案（`_VERIFICATION_CHECKLIST.md`）。
这一轮没有再找三个 subagent 读同一份文档，理由就是那份清单 §8 自己写的：
「三方读的是同一份报告、按同一个清单核，高一致度更可能说明大家都在验报告、
而不是都在验代码」。005 过期的部分是 **§4/§5 的三条实测结论**（锁顺序、`_NoCommit`、
四条路径落盘），这三条的对照物是**代码跑出来的结果**，不是第四个人的读后感。
所以重跑方式是：在副本上把方案真实现一遍，逐条比对。结果是三条全对、多出一条漏项。

**过方案时改掉的七个判断**（详见 §3、§4、§5）：

1. 改动面比方案原来假设的小得多 —— 不存在「散落在各处的调用方」，
   `commit` 参数只被 `ingest.sync()` 自己传，去参数是一处内部重构。
2. 「`start_run` 用独立连接」从**可选**变成**必须**。原方案写的是
   「需要留崩溃痕迹的话」，实际上现在 `start_run(commit=True)` 在真跑路径上
   是立刻落盘的，崩溃痕迹**已经存在**。一旦把整个 sync 收进一个事务并在异常时
   rollback，这行痕迹会跟着被回滚掉 —— 那是**功能倒退**，不是重构。
   痕迹是刚需的原因在 `cli.py:308-310`：`cli status` 每个源只读最近一条 run，
   没有 `running` 那行，崩溃的一次同步会显示成上一次的 `ok`，等于报假账。
3. §3 引的行号是错的，已改。`commit=` 三个形参在 `db.py:137`（`register_source`）、
   `:162`（`start_run`）、`:188`（`finish_run`），不是原来写的 `:44/:64/:83`
   —— 那三行分别是 `PRAGMA foreign_keys=ON`、一个 `for` 循环、一句 `return done`。
4. 「独立连接不会和主连接抢 SQLite 写锁」这条假设**是错的，实测会抢**。
   于是 §4 多出一节「独立连接的三条硬顺序」：先 `register_source` 提交（否则新源
   首次跑 `FOREIGN KEY constraint failed`），再 `start_run` 提交，然后才开主事务，
   `finish_run` 必须等主事务 `commit`/`rollback` 之后（否则 `database is locked`）。
   §5 原来那段骨架按原假设写的，**会死锁**，已整段换掉。
5. `register_source` 也必须走侧连接，不能走主连接。原方案只点了 `start_run`。
   走主连接的话 dry-run 会把 `sources` 那行留下来 —— 就是 `ingest.py:69-72`
   注释里记的那个旧 bug，等于原地踩回去。
6. §3 的假设「收拢后 6 个 `test_dry_run_*` 仍然全绿」**是错的，4 条红**。
   去掉 `commit=` 形参后 dry-run 下 `side is conn`，那两句无条件 commit 落在
   主连接上，结尾 `rollback()` 无事可回，留下 `sources` 一行 + `runs` 一行
   `running`。**方案要的目标本身没问题**，但需要多一层 `_NoCommit` 包装
   （见 §5），加上之后 351 全绿。§5 原来那张三条路径表的 dry-run 行是在
   还没去掉形参的代码上测的，不成立，已重测改掉。
7. §5 骨架里的 `db.connect()` 不能是裸的 —— 它打到 `db.DB_PATH`，也就是
   `data/jobagent.db` 那个 85 MB 真库。仓库里没有 conftest.py、没有任何地方
   monkeypatch `DB_PATH`，照骨架字面量实现再跑测试，测试数据会往真库里写。
   改成从主连接的 `PRAGMA database_list` 取库文件路径。
   另外侧连接从不 `close()`，`sync all` 五个源每轮泄 5 个，包成常驻进程会累积。

8. **§5 骨架里那个 `...` 占位掩盖了一次真实重构 —— 照字面实现只修好一半 bug。**
   （2026-08-09 重跑验证时发现，前七条都是 08-07 过方案时发现的。）

   骨架长这样：`try: jobs = adapter.fetch()` / `...` / `conn.commit()`。
   而当前代码里 `try` **只包 `fetch()`**（`ingest.py:112-118`），写库循环整段在
   try 外面。照骨架字面实现 = 保留原来那个只包 fetch 的 try，于是：

   | 失败路径 | 业务数据 | `runs` 那一行 |
   |---|---|---|
   | `fetch()` 抛（源返回空） | 正确回滚 | `failed` + error ✓ |
   | **写库循环抛（同批重复 external_id）** | 正确回滚 | **`running`，error 为空** ✗ |

   第二行就是 §1 列的触发条件、反馈报告问题 2 的原始 bug。业务数据是干净了，
   但 run 停在 `running` —— `cli status` 读的正是这一行（`ORDER BY id DESC LIMIT 1`），
   于是**崩过的一轮显示成「还在跑」**。方案 §4 要留崩溃痕迹是为了「查得到崩在哪」，
   痕迹里没有 error、状态还是 running，等于痕迹说了半句话。

   `...` 那三个点的实际含义是「把 `:139-303` 全部收进这个 try」，包括空返回判定、
   快照写入、jobs 写入、关闭判定、事件写入、结尾的 commit/rollback。
   这不是填空，是把一个 165 行的函数体重新分块。副本上做完之后：
   重复 external_id → `('failed', 'UNIQUE constraint failed: jobs.source_key…')`，
   jobs 0 行、snapshots 0 行，371 passed。

   **写方案时该把这一句写进去**：「`try` 现在只包 `fetch()`，收拢意味着写库循环
   整段进 try，`fetch()` 那个独立的 try/except 一起并掉」。写成 `...` 的代价是
   实现的人（哪怕是同一个人）会以为那是照抄就行的骨架。

---

## 一、产品设计

### 1. 为什么现在做

**不做会发生什么：** 反馈报告已复现一例事务污染 bug（问题 2），且 `db.py` 和 `ingest.py` 里的大段注释说明这个雷已经踩过至少三次。当前架构下，每加一个写库点都要记得正确传 commit 参数，这类 bug 会反复出现。

**触发条件（已复现）：**
1. 同一批抓取里出现重复的 external_id（翻页期间岗位池变动时会发生）
2. 插入撞唯一约束抛 IntegrityError
3. `ingest.sync` 的 try 只包了 fetch()，没包写库循环
4. 异常抛到 CLI，CLI 捕获后 continue 处理下一个源
5. 下一个源的第一次 commit 把失败源留在事务里的半截数据一起提交

**结果：** 残留半批 jobs 和 snapshots，run 状态永远停在 running。

**根治方案：** 事务边界收拢到 `ingest.sync()` 一处——入口开始、成功提交、任何异常回滚。把散在 `register_source`、`start_run`、`finish_run` 里的 commit 参数全部去掉。

### 2. 用户会看到什么

**修复前（当前行为）：**
```
用户执行 `sync all`
→ 第一个源抓取到重复 id，写库失败
→ CLI 打印 "[red]失败[/red] source1: UNIQUE constraint failed"
→ 继续处理第二个源
→ 第二个源成功，commit
→ 用户看到第二个源成功，但不知道第一个源的半截数据已经混入库
→ 数据库里：第一个源的 run 状态永远 running，部分 jobs 已写入
```

**修复后：**
```
用户执行 `sync all`
→ 第一个源抓取到重复 id，写库失败
→ CLI 打印 "[red]失败[/red] source1: UNIQUE constraint failed"
→ 第一个源的所有数据库操作全部回滚，库里没有任何痕迹
→ 继续处理第二个源
→ 第二个源成功，commit
→ 用户看到：第一个源失败、第二个源成功，数据库状态干净
```

**对 dry-run 的改进：**
修复后，dry-run 模式下整个事务由 `sync()` 统一 rollback，调用方不需要关心每个写函数的 commit 参数。

### 3. 哪些是已核实的，哪些是我猜的

**已核实：**

| 事实 | 怎么核实的 |
|---|---|
| try-except 只包 fetch()，不包写库循环 | `ingest.py:97-103` vs `:168-244` |
| CLI 捕获异常后 continue | `cli.py:69-73` |
| 散落的 commit 参数存在于三个函数 | `db.py:137` register_source、`:162` start_run、`:188` finish_run（原来这行写的 `:44/:64/:83` 是错的 —— 那三行分别是 `PRAGMA foreign_keys=ON`、一个 `for` 循环、一句 `return done`。和下面第 84 行本来就矛盾，2026-08-07 过方案时改掉） |
| db.py 有大段注释解释 commit 参数和 dry-run 泄漏 | `db.py:141-172, :167-172, :190-195` |
| 当前所有写操作都在同一个连接上 | `ingest.sync()` 接收 `conn: Connection` 参数，所有 db 调用用这个 conn |
| `commit` 参数只有 4 处传参，全在 `ingest.py` 自己 | `grep -rn "commit=" --include="*.py" .` → `ingest.py:79 :81 :100 :119`；`db.py` 里另外 4 处命中是注释 |
| 带 `commit` 形参的函数就 3 个 | `db.py:137` register_source、`:162` start_run、`:188` finish_run |
| **成功路径上 `finish_run` 没传 `commit`**，走默认 `True`，紧接着又 `conn.commit()` | `ingest.py:277-278` —— 连着两次 commit，无害但冗余，收拢后这一处自然消失 |
| **写库循环整段没有 try/except** | try 只包 `fetch()`（`ingest.py:97-103`）；`:136-266` 的快照写入、jobs 写入、关闭判定、事件写入全裸奔 |
| 真跑路径上 `start_run` 立刻落盘，崩溃痕迹**已经存在** | `db.py:177-178` `if commit: conn.commit()`，真跑时 `write=True` |
| dry-run 的现有行为被 6 个测试钉住 | `tests/test_ingest.py:267 :274 :289 :300 :319 :328`，全是 `test_dry_run_*` / `test_failed_dry_run_*` |

**假设：**

| 假设 | 如果错了会怎样 | 打算怎么验 |
|---|---|---|
| 没有别的写操作需要「立刻可见」 | 收进事务后它要等到最后才可见，行为变了 | 已核对 `sync()` 全文：除 run 留痕外，其余写操作都只在本轮内被读 |

**已经不成立、从假设里删掉的三条：**

1. 原方案假设「去掉 commit 参数后要改所有调用方，漏改会 TypeError」。
   实测调用方只有 `ingest.py` 自己 4 处，不存在漏改风险。
2. 原方案假设「用独立连接写 run 那一行，不会和主连接抢 SQLite 写锁」。
   **这条是错的，实测会抢。** 见下面 §4「独立连接的三条硬顺序」。
3. 原方案假设「收拢后那 6 个 `test_dry_run_*` 仍然全绿」，还标成
   「本次唯一有回归风险的地方」。**方向对，结论反了：按原骨架实现是 4 条红。**
   已经不是假设了，是实测结论，挪到 §5。要全绿得多一层 `_NoCommit`。
   复核步骤见 §9。

---

## 二、具体实现

### 4. 数据长什么样，空值怎么办

本次重构不改数据形状，只改事务边界。但需要明确一个状态：

| 字段 | 有值时 | 取不到时 | 「没有值」和「不匹配」分开了吗 |
|---|---|---|---|
| run.status | "running" / "success" / "failed" | 不会取不到（建表时有默认值） | N/A |
| run.error | 错误信息字符串 | `NULL` | 是 —— NULL 表示无错误 |

**关键点（过方案时改掉了原来的写法）。**

原方案这里写的是「事务回滚后，run 表里**不应该有任何记录**」。这句是错的，
照它做会造成功能倒退：

现在真跑路径上 `start_run(commit=True)` 是立刻落盘的（`db.py:177-178`），
所以进程被杀、写库炸掉之后，库里留得下一行 `running` —— 这正是
`db.py:165-166` 那条注释要的东西：「崩一次也留得下『这轮开过、没收尾』的痕迹，
否则崩一次就查不到崩在哪」。

把整轮收进一个事务、异常时 rollback，会把这行痕迹一起回滚掉。
于是排查手段从「库里有一行 running，知道崩在哪个源」退化成「什么都没有」。

**所以 run 那一行必须用独立连接写，这是必须项不是可选项：**

| 写的东西 | 用哪个连接 | 异常时 | 为什么 |
|---|---|---|---|
| `runs` 开头那一行 | **独立连接**，单独 commit | **留着** | 崩溃痕迹，回滚了就查不到崩在哪 |
| `runs` 收尾（finish_run） | 独立连接，单独 commit | 写 `failed` + error | 让 `cli status` 说真话，不停在 running |
| sources / jobs / snapshots / events | 主连接，`sync()` 统一提交 | **全部回滚** | 这些是业务数据，半截的比没有更糟 |

dry-run 例外：dry-run 下 run 那一行**也不落盘**。现有 6 个 `test_dry_run_*`
钉的就是这个（`:289 test_dry_run_leaves_no_running_row`、
`:300 test_failed_dry_run_leaves_nothing_either`），所以独立连接只在
`write=True` 时启用，dry-run 仍然走主连接 + 最后 rollback。

**独立连接的三条硬顺序（2026-08-07 过方案时补，原方案缺这一段）。**

上面那张表只说了「哪个写用哪个连接」，没说**顺序**。顺序是硬的：照 §5 那个
骨架直接写会死锁。SQLite 单写者，同一个库文件两个连接同时持写事务必然有一个
失败（3.50.4 / 3.53.1 两个版本实测一致，不是版本问题）：

| # | 约束 | 违反时的实测报错 |
|---|---|---|
| 1 | `register_source` 必须**先单独提交**，才能 `start_run` | 两种报错都真实，看主连接当时有没有持写锁：<br>· 主连接持着未提交的 `sources` 行 → 先撞 `OperationalError: database is locked`（锁在外键检查之前）<br>· 无锁争抢、源确实没注册 → `IntegrityError: FOREIGN KEY constraint failed`（`runs.source_key REFERENCES sources(source_key)`，`schema.sql:28`）<br>（原来这行只写了外键那个，2026-08-07 补上锁那个 —— 按方案描述的场景实际先看到的是锁） |
| 2 | `start_run` 的侧写必须在**主事务开写之前**做完并提交 | `OperationalError: database is locked` |
| 3 | `finish_run` 的侧写必须在主连接 `commit`/`rollback` **之后** | `OperationalError: database is locked` |

跑通的顺序：

```
register_source(主) + commit → start_run(侧) + commit → 主事务写业务数据
  → 成功：主 commit    → finish_run(侧, ok)
  → 失败：主 rollback  → finish_run(侧, failed + error)
```

这个顺序实测走通：`runs` 留下 `('new_src', 'failed', 'boom')`，`jobs` 回滚到 0 行 ——
崩溃痕迹留住了，业务数据全退了，两个目标同时满足。

**约束 1 和 §5 的目标冲突，按这里的写。** §5 原来说「去掉所有 commit 参数，
事务管理只在 `sync()` 一处」，但约束 1 要求 `register_source` 保留独立提交。
准确说法是：**去掉 `commit=` 这个形参**（调用方不再控制提交时机），
三个 runs/sources 相关函数各自在独立连接上提交，`sync()` 只拥有业务数据那**一个**事务。

### 5. 硬约束在哪一行

**事务边界是硬约束，通过代码结构强制。**

原来这里画的骨架是「整个函数体进一个 try，`except` 里 rollback」。
那个骨架**会死锁**：它没有位置放侧连接的写，而 `finish_run` 一旦落在
主连接 rollback 之前就撞写锁（见 §4 约束 3）。真正能跑的形状是这样：

```python
class _NoCommit:
    """dry-run 用：记账照写主连接，但把 commit 吞掉，留给结尾那句 rollback。

    这是去掉 `commit=` 形参的代价所在，也是它比形参强的地方：
    「要不要提交」只在 sync() 这一处决定，调用方不用逐个记得传，
    忘了传也不会静默落盘。
    """

    def __init__(self, conn):
        self._conn = conn

    def __getattr__(self, name):
        return getattr(self._conn, name)

    def commit(self):
        pass


def sync(conn, adapter, *, dry_run: bool = False) -> dict:
    write = not dry_run
    # 侧连接必须跟随主连接的库文件。**不能写 bare db.connect()** ——
    # 它解析到模块全局 db.DB_PATH，也就是 data/jobagent.db 那个真库。
    # 仓库里没有 conftest.py、没有任何地方 monkeypatch DB_PATH，
    # 测试的 fixture 是 db.connect(tmp_path/"test.db")，所以 bare connect
    # 会让测试一边连临时库一边往真库写。
    dbfile = conn.execute("PRAGMA database_list").fetchone()[2]
    side = db.connect(Path(dbfile)) if write else _NoCommit(conn)
    db.register_source(side, ...)           # 记账连接，自己提交（§4 约束 1）
    run_id = db.start_run(side, ...)        # 记账连接，自己提交（§4 约束 2）
    stats = {...}

    try:
        jobs = adapter.fetch()              # 原来这里有独立的 try/except，并掉

        # ↓↓↓ 这一段是【整个函数体的剩余部分】，不是一句省略号 ↓↓↓
        # 空返回判定（失败时直接 raise，不在这里 finish_run）
        # 快照写入 / jobs 写入 / 关闭判定 / 事件写入 —— 全写在 conn 上，一次不提交
        # ↑↑↑ 当前代码里这一段在 try 【外面】，收拢就是把它搬进来 ↑↑↑

        if dry_run:
            conn.rollback()
        else:
            conn.commit()                   # 业务数据只有这一个事务边界
    except Exception as exc:
        conn.rollback()                     # 先退业务数据
        db.finish_run(side, run_id, "failed", 0, str(exc))   # 再写痕迹（§4 约束 3）
        raise
    else:
        db.finish_run(side, run_id, status, len(jobs), err)  # 成功路径在这里收尾
    finally:
        if write:
            side.close()                    # 常驻进程里不关就是每轮泄一个连接
    return stats
```

**这个形状 2026-08-09 在副本上跑过了**（371 passed，四条路径落盘与 §5 末尾那张表
逐格一致，30 轮混合成功/失败之后存活连接净增 0）。两处原来标着「还没跑过、不要照抄」
的地方都定下来了：

- `side.close()` 放 `finally`，成功路径的 `finish_run` 放 **`else`** ——
  `else` 在 `finally` 之前执行，所以连接还在。原来担心的「关掉之后没连接可用」
  是把 `finish_run` 留在 `finally` 之后才会发生的事。
- 业务数据的 `commit`/`rollback` 收在 `try` 的**末尾**（不是函数末尾），
  这样 `except` 里那句 `conn.rollback()` 对「提交失败」本身也成立。

**`...` 那一段是本方案最容易做错的地方，单独说一遍。** 见 §0 偏差 8：
照字面把 `...` 当成「中间省略、照抄即可」，会保留当前那个只包 `fetch()` 的 try，
结果是**写库循环抛异常时 run 停在 `running`、error 为空** —— 本方案要修的 bug
只修好一半（业务数据干净了，但 `cli status` 仍然说假话）。
`fetch()` 原有的独立 try/except 必须并掉，空返回那条失败路径改成直接 `raise`。

三点和原骨架不一样，都是实测逼出来的：

- `register_source` / `start_run` 在 `try` **外面**，因为它们必须先于主事务提交
- `finish_run` 在 `rollback` **后面**，不是 `except` 块开头
- `fetch()` 收进了同一个 `try` —— 现在它有自己的 try（`ingest.py:97-103`），
  合并之后 fetch 失败和写库失败走同一条清理路径
- `register_source` 走的是 `side` 不是 `conn`。**这一处如果写成 `conn` 且无条件提交，
  dry-run 就会漏一行 `sources`** —— 正是 `ingest.py:69-72` 那段注释记录的旧 bug
- dry-run 的 `side` 是 `_NoCommit(conn)`，不是裸的 `conn`。**这一条是 2026-08-07
  实测补的，原来写的「dry-run 走主连接 + 最后 rollback」不够**，见下面。

**为什么 dry-run 必须包一层，不能直接把 `side` 设成 `conn`。**

去掉 `commit=` 形参之后，`register_source`/`start_run` 变成无条件提交。
dry-run 下 `side is conn`，于是这两句 commit 落在主连接上，结尾那句
`rollback()` 无事可回。实测（把方案实现出来跑仓库自己的测试）：

```
真仓库基线                      : 351 passed
side = conn（原方案写法）        : 4 failed / 347 passed
side = _NoCommit(conn)          : 351 passed
```

4 条失败全在 `TestDryRunWritesNothing`：`test_dry_run_leaves_every_table_untouched`、
`test_dry_run_does_not_leak_a_new_source`、`test_dry_run_leaves_no_running_row`、
`test_failed_dry_run_leaves_nothing_either`。落盘结果是 `sources=1` +
`runs=[('running', None)]` —— **就是 `db.py:166-171` 注释里记着的那个「`cli status`
说假话」的 bug 原地踩回去**。

原方案 §3 把「收拢后 6 个 `test_dry_run_*` 仍然全绿」列为唯一有回归风险的地方，
方向对，结论反了：不加 `_NoCommit` 就是 4 条红。

还有一个绕不过去的约束：**dry-run 不能跳过 `start_run`**。
`snapshots.run_id` 是 `NOT NULL REFERENCES runs(id)`（`schema.sql:40`），
而 `ingest.py:136-148` 每条路径都写 snapshots，所以 `runs` 那行必须真的 INSERT、
拿到 id，只是不许提交。这就排除了「dry-run 干脆不记账」这个更省事的形状。

**dry-run 下 `finish_run` 是空操作，这是对的，别当成 bug 去"修"。**
dry-run 时 `rollback()` 已经把 `start_run` 那行 INSERT 退掉了，
后面那句 `UPDATE runs ... WHERE id=?` 命中 0 行（实测 `rowcount == 0`），
所以不写任何东西、也不会被下一个源的 commit 带进库。四条路径实测
（`side = _NoCommit(conn)` 那版，每次都用一条新连接只读已提交状态）：

| 路径 | sources | runs | jobs |
|---|---|---|---|
| 真跑成功 | 1 | `[('ok', None)]` | 1 |
| 真跑失败（fetch 抛） | 1 | `[('failed', '…拒绝按「全部关闭」处理')]` | **0** —— 痕迹留住、业务全退 |
| dry-run 成功 | 0 | `[]` | 0 —— 6 个 `test_dry_run_*` 钉的就是这个 |
| dry-run 失败 | 0 | `[]` | 0 |
| **真跑失败（写库循环抛）** | 1 | `[('failed', 'UNIQUE constraint failed: jobs…')]` | **0** |

（原来这张表只有三行，dry-run 那行写的 `0 / [] / 0` 是**在还没去掉 `commit=` 的
代码上测的**，不成立。去掉形参之后按原骨架实测是 `1 / [('running', None)] / 0`。
2026-08-07 重测改掉。）

**第五行是 2026-08-09 重跑时补的，也是本方案真正要修的那条路径。**
前四行的失败都是 `fetch()` 抛的 —— 那条路径原来就有 try 罩着，所以照骨架实现也对。
而 §1 列的触发条件（同批重复 external_id → `IntegrityError`）是**写库循环**抛的，
它落在原来那个 try 外面。这一行如果不单独测，会漏掉偏差 8 那个「只修一半」的形状：
业务数据 0 行看着完全正常，`runs` 那行却停在 `running`。
触发方式：`sync(conn, Fake([job(1), job(2), job(2)]))`，同批喂两条相同 external_id。

**不能靠调用方传参：** 去掉所有 `commit=True/False` **形参**，调用方不再控制提交时机。
但这不等于「只剩一个 commit」：`sync()` 只拥有**业务数据**那一个事务，
`sources` 和 `runs` 各自在自己的连接上提交（原因见 §4）。

### 6. 判据的粒度

本次不涉及判据。

### 7. 数字的口径

本次不产出新的数字指标。

### 8. 这次明确不做什么

- **不改 dry-run 的语义** —— 仍然是"抓取+写库+回滚"，只是回滚由 sync() 统一执行，不散落在各个写函数
- **不改 start_run 留痕的需求** —— 如果需要留痕（方便排查崩溃），用独立连接单写那一行，不影响主事务
- **不改异常类型和错误信息** —— CLI 看到的错误信息不变，只是内部事务边界更清晰

---

## 三、怎么验

### 9. 验证命令

```bash
# 全量测试通过
uv run pytest -q

# 新增测试：事务污染防护
uv run pytest tests/test_ingest.py::test_transaction_isolation_on_error -v

# 回归测试：dry-run 仍然不落盘
uv run pytest tests/test_ingest.py -k dry_run -v
```

**§5 那三条实测结论是这么测出来的**（2026-08-09 重跑了一遍，三条全部复现：
`side = conn` 是 **4 failed / 367 passed**、加 `_NoCommit` 是 **371 passed**、
四条路径落盘逐格一致。方案原文写的 351 是 004/006 落地之前的基线）。
落地前想复核就照这个走 —— 在仓库副本上改，不动真仓库、不碰真库：

```bash
rsync -a --exclude 'data/' --exclude '.venv/' --exclude '.git/' \
      --exclude 'profile.yaml' --exclude 'screenshots/' \
      --exclude '__pycache__/' --exclude '.pytest_cache/' \
      ./ /tmp/impl005/
cd /tmp/impl005 && uv run pytest -q      # 基线，应当和真仓库一致
# 然后按 §5 改 jobagent/db.py（去掉 3 个 commit= 形参）
#          和 jobagent/ingest.py（侧连接 + 顺序）
uv run pytest -q                          # side=conn 是 4 failed；加 _NoCommit 是全绿
```

排除 `data/` 是硬要求：副本里不要有那个 85 MB 真库，
测试跑飞了也波及不到（顺带 `profile.yaml` 有真实姓名手机邮箱，也不进副本）。

**重跑时另外补的三条（2026-08-09），前两条方案原来没有：**

```bash
# ① 锁顺序：三条硬约束的报错逐条对上（不依赖本仓代码，纯 sqlite 行为）
#    实测 3.51.0：约束 2/3 → database is locked；约束 1 → FOREIGN KEY
#    constraint failed（无锁争抢时）/ database is locked（主连接持锁时）

# ② 写库循环抛异常那条路径 —— 偏差 8 就是这条测出来的
#    同批喂两条相同 external_id：sync(conn, Fake([job(1), job(2), job(2)]))
#    判据：runs 必须是 ('failed', 'UNIQUE constraint failed…')，不是 ('running', None)

# ③ 侧连接不泄漏：30 轮混合成功/失败之后数存活的 sqlite3.Connection
#    判据：净增 ≈ 0（实测起始 1、30 轮后 1）。不关的话会是 +30 左右
```

①②③ 都在副本上跑过。②是这一轮唯一改变方案结论的那条，
所以它进了 §10 的测试表（`test_write_loop_failure_marks_run_failed_not_running`）。

### 落地后的变异测试（2026-08-09）

新测试断言的是「做一半会红」，那就得真做一半试试。在 `/tmp/mut005` 副本上
逐个把代码改坏、跑全量、还原。**7 个变异全部被捕获**：

| 变异 | 改了什么 | 结果 |
|---|---|---|
| ① 没上侧连接 | `side = conn`，`side.close()` 去掉 | **4 红** —— 4 个 `test_dry_run_*`。和落地前重跑的结论一致 |
| ①b 一个事务管所有东西 | ① 再加上三个记账函数都不自己提交 | **6 红** —— 含 `test_run_row_survives_write_failure`。这才是那条测试 docstring 说的形状 |
| ② 按 §5 骨架字面实现 | `try` 只包 `fetch()`，写库循环整段挪出去 | **8 红** |
| ②b 只丢痕迹 | 失败路径 `conn.rollback()` 保留、`finish_run` 去掉 | **3 红** —— 精确复现偏差 8：业务数据回滚正确，run 停在 `running`。**`test_run_row_survives_write_failure` 在这个变异下是绿的**（行确实还在），只有新加的 `test_write_loop_failure_marks_run_failed_not_running` 抓得住 |
| ③ 空返回判定挪出 try | `fetch()` + 空判定都在 try 外面 | **3 红** |
| ④ `side.close()` 只写在 `else` | 失败路径每轮漏关一个 | **1 红**（改判据之后。改之前 **0 红**，见下） |
| ⑤ dry-run 也开真侧连接 | `_NoCommit` 换成 `db.connect(...)` | **5 红** —— 4 个 `test_dry_run_*` + `test_dry_run_opens_no_side_connection` |
| ⑥ 裸 `db.connect()` | 不跟随主连接的库文件 | **32 红**（顺带证明偏差 7 那条不是空担心） |
| ⑦ `register_source` 不提交 | 源那行不落盘 | **1 红** `test_e2e` → `database is locked`。和 §4 约束 1 记的报错对上 |

**变异 ④ 是这一轮唯一一次「测试写错了」。** 原判据是「30 轮之后存活的
`sqlite3.Connection` 对象数没涨」，实测在 ④ 下 **379 passed**，一条都没红。
原因是 CPython 引用计数：`sync()` 的栈帧退掉时那个没人引用的侧连接被立刻回收，
所以「漏关」和「关了」在对象计数上看不出区别。改成 monkeypatch `db.connect`、
记下每个开出来的连接、结束时逐个试 `SELECT 1` 看是否 `ProgrammingError`，
④ 才变红。

```bash
# 变异测试的做法（副本上改，不动真仓库、不碰真库）
rsync -a --exclude 'data/' --exclude '.venv/' --exclude '.git/' \
      --exclude 'profile.yaml' --exclude 'screenshots/' \
      --exclude '__pycache__/' --exclude '.pytest_cache/' \
      ./ /tmp/mut005/
cd /tmp/mut005 && cp jobagent/ingest.py ingest.py.orig && cp jobagent/db.py db.py.orig
# 改一处 → uv run pytest -q → cp *.orig 还原 → 下一处
```

### 10. 测试钉的是哪几条

| 结论 | 测试 | 做一半会红的那条 |
|---|---|---|
| 写库循环里抛异常，业务数据全部回滚 | `test_write_loop_error_rolls_back_business_data`（新建） | 同左 |
| 同一批重复 external_id 不再污染库 | `test_duplicate_external_id_leaves_no_partial_rows`（新建，复现报告问题 2） | 同左 |
| 一个源失败不污染下一个源 | `test_failed_source_does_not_leak_into_next`（新建） | 同左 —— 这条是报告那例 bug 的守门测试 |
| 异常时 run 那一行**留着**并写成 failed | `test_run_row_survives_write_failure`（新建） | **只收拢事务、没上独立连接**时这条红 |
| **写库循环抛异常时 run 是 `failed` 而不是 `running`** | `test_write_loop_failure_marks_run_failed_not_running`（新建，2026-08-09 补） | **照 §5 骨架字面实现、`try` 只包 `fetch()`** 时这条红（偏差 8）。上面那条 `test_run_row_survives_write_failure` 抓不到它 —— 那条只断言「行还在」，而这里行确实还在，只是状态是 running |
| 侧连接不泄漏 | `test_repeated_syncs_do_not_leak_connections`（新建，2026-08-09 补） | `side.close()` 漏掉或放错位置时这条红。**判据换过一次**：数存活的 `Connection` 对象抓不住（变异 ④ 全绿），改成盯 `db.connect` 开出来的每个连接是否 closed。见 §9 |
| 源返回空这条路径没被重构改坏 | `test_fetch_failure_also_marks_run_failed`（新建，落地时加的反向对照） | 空返回原来是 `finish_run` + `raise`，收拢后改成裸 `raise` 统一由 `except` 收尾 —— 改坏了得有人喊。少了它，上一条可以靠「把所有失败都写成 failed」蒙对 |
| dry-run 一个侧连接都不许开 | `test_dry_run_opens_no_side_connection`（新建，落地时加的反向对照） | `_NoCommit` 换成真侧连接时这条红。`TestDryRunWritesNothing` 那几条也会红，但它们说的是「库里多了行」，这条说的是「为什么多」 |
| dry-run 什么都不落盘（含 run 那一行） | 已有 6 条：`test_dry_run_leaves_every_table_untouched`、`test_dry_run_does_not_leak_a_new_source`、`test_dry_run_leaves_no_running_row`、`test_failed_dry_run_leaves_nothing_either`、`test_dry_run_still_computes_stats`、`test_repeated_dry_runs_stay_bootstrap` | **独立连接误用在 dry-run 上**时这些红 |
| 空结果仍然拒绝按全部关闭处理 | 已有 `test_empty_fetch_raises_not_closes_everything`、`test_empty_fetch_ok_when_adapter_says_so`、`test_authoritative_empty_does_not_mass_close` | 同左（回归） |

**这次「做一半」的形状是明确的，值得单独写下来。**

收拢事务是一个改动，run 留痕用独立连接是另一个改动。只做前一半的话：
业务数据回滚正确、报告那例 bug 也修好了、`test_dry_run_*` 全绿，
**唯一挂的是 `test_run_row_survives_write_failure`**。
没有这条测试，这个半成品会看起来完全正常，而代价是排查能力静默退化
（本仓库 001 §12 那个「索引两边都留」的坑是同一个形状：常见路径全绿）。

**反向用例：** 方案原来写的是一条 `test_transaction_isolation_on_error`，
落地时拆成了两条，因为它混了两件事：

- 「一个源崩了不污染下一个源」→ `test_failed_source_does_not_leak_into_next`
- 「崩掉的那轮留下 failed 痕迹」→ `test_run_row_survives_write_failure`
  + `test_write_loop_failure_marks_run_failed_not_running`

拆开的理由是它们的失败原因不一样：前者红说明事务没收拢，后者红说明侧连接没上
或者痕迹没写。合成一条的话，红了还得再去分辨是哪一半坏了。

**如果做一半会怎样（落地后逐条变异实测，见 §9）：**
- 去掉 `commit=` 形参但三个记账函数也不自己提交 → **变异 ⑦：`test_e2e` 撞
  `database is locked`**（不是「测试全挂」—— 方案原来的预测偏严重）
- 只改了部分写函数 → 漏掉的调用点传 `commit=` 会 `TypeError`。仓库里只有
  `ingest.py` 传过这个参数，所以实际不存在「部分」的空间

---

## 四、复盘

### 11. 方案和实现差在哪

| 方案里写的 | 实际做成什么样 | 为什么改了 |
|---|---|---|
| `try: jobs = adapter.fetch()` / `...` / `commit()` | 整个函数体（原 `:139-303`）收进同一个 `try`，`fetch()` 原来那个独立的 `try/except` 并掉 | 那个 `...` 不是填空，是 165 行的重新分块。照字面实现只修好一半 bug（偏差 8） |
| `db.connect()` | `db.connect(Path(conn.execute("PRAGMA database_list").fetchone()[2]))` | 裸的会打到 `data/jobagent.db` 那个真库（偏差 7，变异 ⑥ 实测 32 红） |
| `start_run` 用独立连接「如果需要留痕迹」 | 必须，且 `register_source` 也得走侧连接 | 痕迹已经存在，收拢后回滚掉它是功能倒退（偏差 2、5） |
| dry-run 靠调用方 `rollback()` | 多一层 `_NoCommit` 包装 | 去掉 `commit=` 形参后两句提交会落在主连接上，`rollback()` 无事可回（偏差 6） |
| §10 六条新测试 | 八条 | 多的两条是反向对照：`test_fetch_failure_also_marks_run_failed`、`test_dry_run_opens_no_side_connection`。理由见 §10 |
| 一条 `test_transaction_isolation_on_error` | 拆成三条 | 原来那条混了「不污染下一个源」和「留下 failed 痕迹」两件事，红了分不清哪一半坏了 |
| 「做一半 → 测试全挂」 | 变异 ⑦ 只红 1 条（`test_e2e` 撞 `database is locked`） | 预测偏严重。真实后果比「全挂」隐蔽 —— 只有跑到真实多源场景才炸 |

### 12. 实现中踩到的坑

**测试自己写错了一次，而且是全绿的那种错。** `test_repeated_syncs_do_not_leak_connections`
原来数存活的 `sqlite3.Connection` 对象数，判据是「30 轮后没涨」。把
`side.close()` 从 `finally` 挪到 `else`（失败路径漏关）之后，它 **379 passed**。
CPython 引用计数在栈帧退掉时就把那个没人引用的连接回收了，所以「漏关」和
「关了」在对象计数上没区别 —— 这个判据测的是垃圾回收器，不是我的代码。
改成 monkeypatch `db.connect` 记下每个开出来的连接、结束时逐个试 `SELECT 1`
看是否 `ProgrammingError`，变异才变红。

**重复 external_id 这个触发器有个前提条件，第一次写测试时漏了。**
它只在 external_id **对库来说是新的**时候才撞 UNIQUE —— 库里已经有的走
UPDATE 分支，撞不到。所以循环里复用同一对 id 时，第二轮之后就不再抛异常了
（`DID NOT RAISE`）。每轮换新 id 才行。

**探针脚本用裸 `except` 掩盖过这件事。** 我先写探针确认失败行为、再写测试，
探针里失败那一路是 `try: sync(...) except Exception: pass`，于是「这一轮没抛」
和「抛了并被吞掉」看起来一样。测试里用 `pytest.raises` 才暴露。
探针脚本也该用严格断言，不然它验证的是「没崩」而不是「行为对」。

### 13. 如果重来，方案里该提前写上哪句话

**「骨架里不许出现 `...`。」** 要么写全，要么写清楚省略的是哪几行、
省略的部分要不要动结构。偏差 8 的全部成因就是三个点看起来像「照抄就行」。

**「测试的判据必须能被变异证伪，写完就去改坏一次试试。」**
本方案 8 条测试里 7 条一次写对，第 8 条全绿但测的是垃圾回收器。
没有变异这一步，它会作为「侧连接不泄漏」的证据留在仓库里 ——
而它证明不了这件事。判据是「资源被正确释放」时尤其要小心：
Python 会替你收拾大部分烂摊子，于是「漏了」和「没漏」经常看不出区别。
