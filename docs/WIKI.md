# Wiki：过程记录

> **这份文档的职责：记录需求背景、调研结论、关键决策及原因、替代方案、
> 实验结果、问题复盘、版本变化。**
>
> **它不是当前实现的依据** —— 那是 `docs/SPEC.md`。这里的东西可能已经过时，
> 而过时的条目**标注证伪、不删除**：删了就没法验证后来的结论是不是编的。
>
> **未经审核的条目不许进 Agent 知识库**（`docs/kb/`）。判断标准见 `docs/kb/README.md`。

索引：
- §1 需求背景
- §2 关键决策
- §3 调研结论（含已证伪的）
- §4 实验结果
- §5 问题复盘
- §6 版本变化

---

## 1. 需求背景

一个人手工刷校招的失败模式不是「累」，是**漏**：岗位放出来那几天没刷到就没了。
所以产品的核心不是「聚合岗位列表」，而是**「哪些岗位新出现了」这条事件流**。

这决定了两个看起来奇怪的设计：为什么要存 `snapshots`（每轮原文都留，
不然漏报无法排查）、为什么误关闭比漏报关闭更严重（假 `job_closed` 直接
摧毁信任，而漏报只是晚知道一天）。

## 2. 关键决策

### 2.1 按 ATS 分层，不按公司分层

**决策**：适配器按招聘系统（feishu / moka / 北森）建，不按公司建。
**原因**：评审提的问题——中厂大多用第三方 ATS，同一 ATS 的租户前端是同一套应用。
实测证实：一个 `FeishuAdapter` 摊了 4 家公司，加一家的边际成本 = 配置里加一行。
**替代方案**：一家一个适配器。放弃原因是边际成本线性增长，且中厂数量远大于大厂。
**依据**：`docs/ATS_RESEARCH.md` 结论二。

### 2.2 代投闸门从提示词改成 API 形状

**决策**：提交必须带 `confirm_token`，拿不到 token 就调不到提交那一步。
**原因**：提示词约束的是倾向不是能力。不可逆动作不能靠模型记得住。
**替代方案**：提示词里写「请先向用户确认」。放弃原因见 `plans/001 §5`。

### 2.3 判不出的值写 `None`，绝不兜底

**决策**：`job_family` 判不出写 `None`，不写 `"other"`；`grad_year` 缺失走三态。
**原因**：「没有值」和「不匹配」混成一个分支，会静默丢岗位——用户永远不知道
它存在过。飞书上判不出族的比例高达 48.7%（nio），兜底成 `other` 等于
让一半岗位从按族筛里消失。
**依据**：`plans/002 §4`。

### 2.4 `--dry-run` 必须真的只读

**决策**：`db.register_source` / `start_run` / `finish_run` 都接 `commit` 参数。
**原因**：dry-run 靠 `conn.rollback()` 实现，而这两个函数原来无条件 commit，
commit 过的东西回滚不回来。表现是 `sync --dry-run` 往真库留下一行永远
`running` 的 run，而 `cli status` 取 `ORDER BY id DESC LIMIT 1`——于是一个
上一轮真实成功、有 795 条岗位的源，状态栏显示 `running` / 抓取 0。
**教训**：凡是「用来验证的东西」，它自己也得有测试。这个 bug 能活下来的
全部原因是 `dry_run` 在测试里一次都没被跑过。

### 2.5 校招门户按 `source_key` 分开，不合进租户

**决策**：`feishu:<tenant>:<portal>`，社招和校招各一个源。
**原因**：合在一起会让关闭判定的分母被混——校招门户整个下线（换届之间很正常）
时消失比例 627/2704=23%，低于 0.4 守卫，627 条会被静默关闭。分开之后是
100%，守卫必然触发。
**依据**：`plans/003 §6`。

## 3. 调研结论

### 3.1 仍然成立

| 结论 | 依据 |
|---|---|
| 厂商 API 对我们是关的，只能走租户公开前端 | `ATS_RESEARCH.md` 结论一 |
| 飞书接口免鉴权、`limit=500` 可用、深翻不重复 | `plans/002 §3` |
| **UA 门**：非 Mac UA → HTTP 405 + 空 body | `plans/002 §3`「UA 门」。排掉了限流、其他请求头、简单子串规则；**没**定位到按哪一项判 |
| 假租户 = HTTP 400 + 非 JSON；真租户 0 岗位 = `code=0` + `count=0` | `plans/002 §6` |
| 族判不出率**按公司**差得很远，不是系统属性 | `plans/003 §0` 偏差 8。校招门户实测 6%~44%（字节 6 / 商汤 9 / 小鹏 37 / 蔚来 44）。~~002 §7 写的 16.9%~48.7% 是社招池四租户的数~~，已在 §3.2 标注。**2026-08-06 17:07 轮重测为 6%~45%**（字节 6 / 商汤 9 / 小鹏 **36** / 蔚来 **45**）—— 两端都动了 1，是岗位池漂移不是改错，「按公司差」这个结论没变。**别把区间端点当常量抄** |
| 届别写在标题里，不在结构化字段里 | `plans/002 §0`「届别」。`plans/004` 把它做成了第二条观测通道：标题必须带「届」字才解析，且**永不返回「不限」**（`[]`）。主库 348 条标题带「届」（小鹏 343 + 蔚来 5），全部是 27 届 |
| 「信息不全」要分维度，不是一个布尔开关 | `plans/004 §2`。只差届别和连岗位族都判不出，可信度差得远。四档实测（2026-08-10，届别第三通道落地后）：严格 **612** / 放开届别 **1952** / 放开族 **789** / 全放开 **2533**；004 那轮是 95/1952/198/2533 |

### 3.2 已证伪（保留原文，标注为错）

| 曾经写的 | 实际 | 错在哪 |
|---|---|---|
| ~~「飞书这个口子没有校招」~~ | 校招 1222 条（nio 627 / xiaopeng 436 / sensetime 159）| **观测对，取样口径错**：不带 `website-path` 打接口拿到的是社招池，我在社招池里数了四遍「没有校招」 |
| ~~「校招门户的接口找不到，现在写等于编」~~ | 同一个接口 + `website-path` 请求头 | 力气全花在猜路径和猜 body 参数上，一次都没试过换请求头 |
| ~~「源站分类两个字段按租户二选一且互斥」~~ | 按**门户**，且 `xiaopeng/398875` 上两个同时有值 | 又一次把一个视图的性质当成数据源的性质 |
| ~~「判不出族 27%」~~ | 全量 48.5%（nio）| 200 条样本的比例被当成接口的性质 |
| ~~「`job_post_info` 整个对象全 null」~~ | `experience`/`required_degree` 等 6 个字段有值 | 只看了几条 |
| ~~「44 个城市，全部原样透出」~~ | 全量 184（nio）/60（xiaopeng），`中国香港`→`香港` 会改写 | 同上 |
| ~~「校招共 1557 条」（口头报给用户的）~~ | **1222**（2026-08-05 三家）| `campus`(436) 和子门户 `398875`(335) 相加，而交集 335 = 完全包含。**两个键不等于两批数据** |
| ~~「`apply_url` = `https://<host>/position/<id>`」~~（`plans/003 §4` 当口径写着）| **必须带门户段**：裸形状是 404，body 9 字节（nio/xiaopeng/bytedance/sensetime 一致，2026-08-06）| **方案里的「口径」栏没有一行是真打开过链接的**。表格排版会让假设看起来像已核实 —— 核对库里 4810 条链接因此全是坏的，而这一列的唯一用途就是给人点开核对 |
| ~~「族判不出率是飞书源的性质，17%~49%」~~ | **6%~44%，按公司差**（字节 6 / 商汤 9 / 小鹏 37 / 蔚来 44，同一份 `TITLE_RULES`）| 差异来自标题写法（蔚来大量「实习-NSC机电技师」这类内部岗名），不是规则坏了。**这是公司属性，不是系统属性**。〔2026-08-06 17:07 轮重测 **6%~45%**：小鹏 37→36、蔚来 44→45。区间端点会漂，结论不漂 —— 这一行的 6%~44% 保留原文，是当时的实测值〕|
| ~~「一个校招门户 = 一个届别」~~（没写进文档，**写进了代码**：`adapters/tencent_join.py:44` 的 `GRAD_WINDOW = {"campus":"26","intern":"27"}` 就是这个假设）| 小鹏一个 `campus` 门户上 **27 届 343 条和 26 届 2 条并存** | `plans/004 §9 命令 B`。错法还是那个老的：**把一个视图的性质当成数据源的性质**（第八种口径错）。「门户」是入口不是届别标签，同一个入口下可以挂多届。**这条目前只证伪了假设，没修代码** —— 腾讯 805 条的届别仍然是推断的，要动得先去门户上看页面写的是哪一届，见 `plans/004 §8` |

**这些错法归成一类，是「口径错」的第八种**（前七种在 `ATS_RESEARCH.md`）：
**把一个视图的性质当成数据源的性质。** 处方：拿到一批数据先问「这是哪一个池」，
再问「这批里有什么」。

`apply_url` 那条是第九种：**把上一个人的假设当成已核实的口径。**
处方：写进方案的每个 URL 形状，落地前打开过一次。

### 3.3 待查（不许当结论用）

- **「不带 `website-path` 那个池」是什么？** 三个池两两互不包含
  （nio: 2250 / 2077 交 1737；haidilao: 52 / 34 交 9）。已排除「每次调都不一样」
  （同一查询连打三次，id 集合三次全等）。**现在给它编解释就是编。**
- `website-path` 具体按什么校验（`code=-9000003` 的判定规则未知）。
- UA 门按 UA 的哪一部分判，未定位，也没拿真浏览器复核。
- Moka / 大易 / 北森的租户页格式没实测，现在写 `fetch()` 是凭想象。

## 4. 实验结果

### 4.1 `website-path` 全量翻页（2026-08-05）

12 个门户逐个翻到底，**每个都 `rows == count` 且 id 全 unique**：

| 租户 | 不带头 | index | campus/edu | 子门户 |
|---|---|---|---|---|
| nio | 2250 | 2077 | 627 | —— |
| xiaopeng | 1642 | 1659 | 436 | `398875`=335 |
| sensetime | 71 | 无 | 159（`edu`）| —— |
| haidilao | 52 | 34 | 无 | —— |

id 交集：校招 ∩ 社招 = **0**（三家全部组合）；`campus ∩ 398875` = **335**（全含）。

**2026-08-06 复跑的数**：sensetime `edu` 160、nio `index` 2078、
不带头 2249。数字自己在动（同一天内 nio 从 2250 变 2249），
**验接口正常要看 `code` 和门户是否存在，不看数字对不对得上**。

### 4.1b 找海底捞替补（2026-08-06）

海底捞没有校招门户，要换一家。三种探法只有一种管用：

| 探法 | 结果 |
|---|---|
| 猜租户 slug（18 个）| **16 个 `JSONDecodeError`**。和猜门户路径、猜 body 参数是同一种错法：猜命名空间 |
| 按域名 slug 认归属 | 早先就栽过（`luckin` 页面自称「加入狂浪俱乐部」）|
| **打开页面看它自称什么** | 管用。筛出 15 个真租户，其中 2 个有校招门户 |

留下 **`bytedance` 字节跳动**：campus **7395** 条，全量翻页 7395 unique id，
`{campus: 2048, intern: 5347}`，判不出族 416（**6%**，四家里最低 —— 标题最规范）。
备选 `chagee` 霸王茶姬 campus 只有 12 条，太薄，撑不起主场景。

### 4.1c `apply_url` 形状（2026-08-06 首测，2026-08-10 修正）

| 形状 | 结果 |
|---|---|
| `/position/<id>` | **404，body 9 字节**（nio / xiaopeng / bytedance / sensetime 一致）|
| `/<portal>/position/<id>` | HTTP 200（192KB~879KB），但**渲染「页面不存在」** |
| `/<portal>/position/<id>/detail` | 渲染岗位正文（2026-08-10 四租户实测）|

**中间那一行 2026-08-10 被证伪了**：当初记的「200」是对的，
结论「所以这个形状能用」是错的 —— 这些页面是客户端渲染 SPA，
**404 发生在渲染层，HTTP 照样 200 且 body 有 200KB**。
库里 8594 条飞书链接因此全是死的，见 `plans/010`。
判死活的判据只能是渲染后的正文里有没有「页面不存在」。

**归属仍然没验到**：任何 id（含 `0` / `abc` / 19 位乱填数字）配上门户段
会不会也渲染出正文，没试过。所以「校招 id 必须配校招门户前缀」
依旧只是按门户构造，没有独立验证。

**这一节自己就是「留了缺口没人管」的样本**：下面那段「要验得跑真浏览器看
network」是 2026-08-06 写的，写得很准确，然后躺了四天。真去跑的时候
第一个发现就是形状错了。教训进了 `plans/010 §13`。

**归属没验到，写在这儿免得下一个人以为验过了**：页面是客户端渲染的 SPA，
任何 id（含 `0` / `abc` / 19 位乱填数字）都回同一个 200 外壳，标题不在 HTML 里；
详情 XHR 没找到（15 个 JS bundle 里搜不到 `/api/v*`）。
所以「校招 id 必须配校招门户前缀」**只是按门户构造，没有独立验证** ——
200 只证明路由存在。要验得跑真浏览器看 network。

### 4.2 被排除的假设（记下来免得下一个人再试）

| 试过什么 | 结果 |
|---|---|
| `portal_type` / `portal_entrance`（query + body，值 1/2/6）| count 不变。真浏览器请求里有，但是陪跑的 |
| `process_type` / `job_process_type` / `website_id` / `portal_website_id` | count 不变 |
| 请求头 `x-website-id` / `website-id` | count 不变 |
| 6 个猜的路径（`/api/v1/campus/search/job/posts` 等）| 全 404 |
| 校招门户 HTML 里找内联岗位数据 | 没有，是客户端渲染 |
| `luckin` / `horizon` 当瑞幸/地平线 | **归属错**：页面自称「加入狂浪俱乐部」/「加入汉森」 |

### 4.3 腾讯（`tencent_join`）的校招/实习构成

库内 801 条：`campus` 366 条全是 `grad_year=26`（上一届存量），
`intern` 435 条全是 `27`。**27 届秋招尚未开放**，所以用户看到「都是实习岗」是对的。

## 5. 问题复盘

各方案 §12 是权威版本，这里只放跨方案的模式：

| 模式 | 出现过几次 | 处方 |
|---|---|---|
| 拿一次采样的性质当接口的性质 | ≥8 | 计数带日期，区间带样本来源 |
| 把一个视图的性质当数据源的性质 | 1（这次）| 先问「这是哪个池」 |
| 两个键当成两批数据 | 1（这次）| 报数前算交集 |
| 改一半：只改了一侧的守卫 | 2 | 守卫改在哪一层，就在那一层再钉一次测试 |
| 用来验证的工具自己没被验证 | 1 | `--dry-run` 也要有测试 |
| §12 引用了不存在的测试 | 1 | 回填时把那条命令跑一遍 |
| **把上一个人的假设当成已核实的口径** | 1（`apply_url` 形状）| 方案里的每个 URL / 形状，落地前真打开一次。**表格排版会让假设看起来像已核实** |
| **猜命名空间**（门户路径 / body 参数 / 租户 slug）| 3 | 不猜，读页面说自己是什么 |

## 6. 版本变化

**这张表停在 `plans/012`（2026-08-10）。** 013 之后逐轮记录搬去 `docs/plans/NNN-*.md`
（每份带 §11–§13 复盘），合并视图看 `CHANGELOG.md` 的 `[未发布]` 段。

不给 013–019 补进这张表，是因为现在写的不会是那几天的视角 —— 这张表的价值在
「当天怎么看这件事」，事后补写出来的是复盘，两者混在一列里就分不清哪句是当时的判断。
每行里的用例数同理：那是那天的真值，不随代码更新。

| 日期 | 变化 |
|---|---|
| 2026-08 | M1–M6 打通；`plans/001` 代投闸门回填 |
| 2026-08-05 | `plans/002` 飞书采集落地，293 passed；修 `--dry-run` 写库泄漏 |
| 2026-08-05 | 用户实地核对指出 4/5 入口是社招站；发现 `website-path`；开 `plans/003` |
| 2026-08-05 | 建立三层文档：`SPEC.md` / `WIKI.md` / `kb/` |
| 2026-08-06 | `plans/003` 落地，320 passed：校招门户按 `website-path` 采集、`source_key` 加门户段、`_recruit_type()` 先看 parent、自定义域名。核对库 4810（社招）→ **9419**（校招 8618 + 腾讯 801）|
| 2026-08-06 | 修 `apply_url` 缺门户段（**已落的 4810 条链接全是 404**）；海底捞（无校招门户）换成字节跳动；`kb/job-fields.md` v2、`kb/company-portals.md` v2 |
| 2026-08-06 | `plans/004` 届别补全落地，**351 passed**：标题作第二条观测通道（必须带「届」字，**永不返回「不限」**）、`Verdict.missing` + `MISSING_DIMS` 四个稳定键、`--allow-missing` 按维度放宽。严格档 **41 → 95**（+54 全是小鹏标题里的「27届」）。`SPEC.md` 同步，`kb/job-fields.md` v3、`kb/company-portals.md` v3 |
| 2026-08-06 | 上面 2026-08-06 那行的 **9419（8618 + 801）是 00:43 轮的数**；16:50 / 17:07 两轮重测为 **9399**（飞书 8594 + 腾讯 805，蔚来 +7 / 商汤 +1 / 小鹏 −5 / 字节 −27）。**是重测漂移不是改错**。同时纠了 `kb` 里的列名口径：那 8618/8594 是**门户下开放合计（含实习）**，不是 `recruit_type=campus` —— 飞书 8594 里只有 2695 条 campus、5899 条 intern |
| 2026-08-09 | `plans/005` 事务边界收拢落地，**379 passed**：`ingest.sync()` 包成单事务，加 `_NoCommit` wrapper 支持 dry-run；修 `--dry-run` 写库泄漏（register_source / start_run 抢先 commit）|
| 2026-08-09 | `plans/006` 城市差异检测落地，**387 passed**：cities diff 进 snapshot、`_cities()` 正规化集合；同步 `SPEC.md`、`kb/job-fields.md` v4 |
| 2026-08-09 | `plans/007` 届别按招聘标签推导落地，**379 passed**（改 fixture，重跑）：应届 = 入口届别、实习 = 不限；修 `test_known_labels` / `test_unknown_type` 两条用例 |
| 2026-08-09 | `plans/008` 届别换季刷新落地：`refresh-grad-year` 命令刷新 in-place，腾讯 27→26 共 93 条实习行；`CURRENT_CAMPUS_YEAR` 加注释 |
| 2026-08-09 | `plans/009` 按 projectId 分桶替代 recruitLabelName 字符串匹配落地，**401 passed**（新增 `test_pid_2_is_local_exception`）；`plans/007` 判据标注已被 009 替代 |
| 2026-08-10 | `plans/008` 过期告警落地：`cli.py::_check_grad_year_staleness()` + `status --check-grad-year`；从 CDN 抓 `Project_CampusSubtitle` 与 `CURRENT_CAMPUS_YEAR` 对比，粒度 `source_key × recruit_type`；实测 ✓（站点=26，代码=26）|
| 2026-08-10 | `plans/010` 飞书代投落地，**435 passed**（401→435）。**顺带修了一个 P0**：飞书 `apply_url` 少 `/detail`，库里 8594 条链接全是死的 —— HTTP 200 但渲染「页面不存在」。新增 `ingest.repair_apply_url()` + `repair-apply-url` 命令（单列原地改、不碰指纹，`job_updated` 107→107 没变）。`FeishuSubmitter` 一个类管四租户，本轮做到登录门为止（表单要真登录才看得见，`FORM_FIELDS` 故意留空）。`kb/job-fields.md` v4、`kb/company-portals.md` v4 |
| 2026-08-10 | `plans/011` 届别第三通道落地，**450 passed**（435→450）。届别原来只有两条通道（结构化字段、标题），第三条是**招聘项目名** `job_subject.name.zh_cn` —— 素材从采集第一天就在快照里，只是没人读。`feishu._grad_year_from_subject()` 采集时推出并**写回** `grad_year` 列（沿用「必须含「届」字」「永不返回不限」两条严档），存量用 `refresh-grad-year` 单列原地刷新。严格档 **95 → 612**（6.4×）、放开族档 198 → **789**，而**放开届别 1952 与全放开 2533 纹丝不动** —— 通道只在档之间搬行，没扩大岗位池；`job_updated` 107→107，零假事件。飞书填充率 0/8594 → **2459/8594**（bytedance 28% / nio 49% / sensetime 45% / xiaopeng 0%，它的届别在标题里）。**根因是一句写窄了却被读宽的话**：kb v3 的「飞书结构化字段里没有届别」对 `grad_year` **这一列**准确，但它躺在「已核实为空」表里，被读成「这个源没有届别」，害届别补全停了四天。`kb/job-fields.md` v5 把那张表整表改写成「每行写清验的是哪个命题 + 哪天验的」并删掉该行（作为反例留在正文）。`SPEC.md` 同步（含补记 009/010 两轮漏掉的变更行）|
| 2026-08-10 | `plans/012` 投递记录出口落地，**464 passed**（450→464）。`applications` 表从建库起就在写，但**没有任何命令能读它** —— `cli status` 只读 `sources`/`runs`，`cli jobs` 只读 `jobs`，于是那 14 条投递记录里的拦截原因和截图路径一直是黑的。新增只读命令 `cli applications [--status/--source/--limit/--funnel]`。**顺带更正了两处我自己在方案里写错的「已核实」**：①「14 条全部卡在登录门」——实际 **10 条登录门 + 4 条「未找到申请按钮」**，而那 4 条比同一岗位的登录记录**更早**（同一个 `external_id` 先报找不到按钮、后报要登录），说明选择器问题当时已修掉，**是历史不是现存卡点**；②「`job_id` 没有 FK，孤儿行真会发生」——`schema.sql:104` 明写 `REFERENCES jobs(id)` 且 `db.py:44` 开了 `PRAGMA foreign_keys=ON`，正常路径下孤儿行**插都插不进来**（测试造孤儿必须显式关 FK）。`LEFT JOIN` 仍然保留，理由换成「唯一能产生孤儿的路径是人工修库/恢复备份，而那恰好是最需要这批记录还在的时刻」。另外 14 次尝试只覆盖 **7 个岗位**（腾讯一个岗位重试 7 次），所以漏斗把**尝试次数和岗位数分开报**、按原因分组、每档带「最近一次」时间，且**不报成功率**（0/14 的分母全是「还没试到那一步」，`blocked` 是信息不全不是不命中）。三条「写错了也全绿」的守卫用**变异测试**验过真会红。`SPEC.md` §2 补 M7 与命令清单 |
