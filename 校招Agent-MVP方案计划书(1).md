# 校招 Agent · MVP 方案计划书

> 版本：v0.1（初稿）
> 目标：7 天内完成 MVP，在 3–5 家重点公司上跑通完整流程，服务不上云

---

## 一、背景与问题

校招拼的是时间差。头部公司提前批 7 月就开，8 月底简历基本筛完；同一家公司不同 BG、不同岗位族还会分批放出来，入口经常不在同一个页面上。

一个认真准备的学生，目标公司池通常在五十到一百五十家。靠人力每天巡检官网、蹲公众号、刷群消息，是纯消耗，而且一定会漏。

现有渠道的缺口：

| 现有渠道 | 缺什么 |
|---|---|
| 牛客 / 实习僧 / 应届生求职网 | 有岗位库，但推送是广播式的，不按个人画像做增量；平台的目标是留住你刷，不是给你最短路径 |
| 招聘公众号 / 群 / 小红书 | 时效尚可但噪声极大，无结构、无法检索，覆盖全凭运营者兴趣 |
| 公司官网订阅 | 覆盖单一，而且大部分公司根本没有这个功能 |

投递本身也是苦活。一份校招表单二三十个字段，教育经历、实习、项目、家庭成员、奖惩、开放题，填一家二十分钟，投五十家就是十几个小时的机械劳动。

---

## 二、价值主张

产品有两块，发现和执行，分量相当。

> 发现 → 筛选 → 代投 → 跟踪

### 发现：公开不等于易得

岗位信息是公开的，但公开只是说理论上能拿到。学生实际要付的成本有三段：找入口，上百家公司官网结构各异，招聘页常常藏得很深；持续巡检，开放时间没法预测，只能天天刷，一天不看就可能错过；从噪声里筛，牛客、公众号、群里给的是原料，不是「跟我有关的变化」。

现有渠道没有真正解决这个问题，只是让人以为解决了。所谓免费替代，实际是用学生的时间去换，而校招季学生最缺的就是时间，那些时间本该用在笔试和面试准备上。

还有一点容易被忽略：两侧的失败代价并不对称。漏掉一家心仪公司的开放窗口，损失是一整年的机会，没法补救；执行环节出错，大不了手动重填一遍。按这个标准，发现侧的质量要求其实更严，覆盖率和时效也因此成了全项目最重的工程投入。

### 执行：没有替代方案的体力活

填表这件事目前没有任何现成工具能代劳。用户对「有人帮我填完」的付费意愿很高，这点不需要论证。

### 两者的关系

| | 发现侧 | 执行侧 |
|---|---|---|
| 使用频率 | 每天 | 集中在投递期 |
| 产品作用 | 留存与日活的来源 | 价值峰值的来源 |
| 失败代价 | 不可逆 | 可补救 |
| 工程重心 | 覆盖率与时效 | 表单泛化能力 |

只有发现，用户拿到清单还得自己填几十份表；只有执行，用户不知道该投什么。定价上按整体打包，不做「信息免费引流、执行收费」的切分。

### 壁垒在哪

信息本身不是壁垒。真正难复制的是三件事：及时性所需要的持续工程投入；用户画像和投递记录攒起来之后的迁移成本；历年岗位开放时间线，这份数据将来能做预测式提醒，把「通知已经发生的」变成「预告要发生的」。

---

## 三、产品思路

四个动作，每一个都要说得清省了多少时间。

1. **建档一次。** 简历结构化加目标画像（届别、岗位族、城市、公司池），一次投入二十分钟，后面全程复用。
2. **每日增量。** 只告诉你相对昨天新增了什么。这一条决定了数据模型是事件流（新开放、批次启动、即将截止、已关闭），而不是岗位快照。
3. **随时追问。** 自然语言查在投窗口、查某家进度、查时间冲突。
4. **批量代投。** 选中 N 个岗位，Agent 逐个打开、填表、生成开放题，停在提交前让用户确认，点一次投一个。

### 投递跟单怎么做

跟单不用当成独立功能来做，它是代投的副产品。分三层看：

| 层级 | 数据来源 | 成本 | 何时做 |
|---|---|---|---|
| L1 投递记录 | 我们代投的，投了什么、何时投，天然就知道 | 近乎为零 | MVP 就有 |
| L2 状态流转 | 笔试邀约、面试通知、感谢信几乎全走邮件和短信，授权后解析邮箱 | 低，一套逻辑通吃所有公司 | 二期 |
| L3 官网进度 | 登录各家候选人中心去读 | 高，每家一个适配器 | 性价比最低，可能永远不做 |

L1 加 L2 已经能覆盖九成诉求，L3 可以放弃。

---

## 四、方案框架

### 4.1 功能模块

| 模块 | 职责 | 一句话 |
|---|---|---|
| M1 采集 | 从官网 / ATS 拉取校招岗位原始数据 | 拿到「有什么」 |
| M2 归一与增量 | 结构化、去重、判定新增/截止/关闭 | 算出「变了什么」 |
| M3 用户档案 | 简历结构化 + 求职画像 | 定义「我是谁、我要什么」 |
| M4 匹配订阅 | 画像 × 岗位增量 → 每日推送内容 | 决定「推什么给我」 |
| M5 交互与调度 | skill/MCP 封装、定时触发、自然语言查询、输出通道 | 用户摸得到的那一层 |
| M6 代投执行 | browser-use 填表、开放题生成、提交前确认 | 把活干了 |
| M7 投递记录 | 投了什么、何时投、什么状态 | M6 的副产品 |

### 4.2 分层结构

```
┌───────────────────────────────────────────────┐
│  M5  交互与调度                                │
│  skill / MCP · 定时任务 · 对话查询 · 输出通道    │   ← 用户触点
└───────────────┬───────────────────────────────┘
        ┌───────┴────────┐
┌───────▼───────┐  ┌─────▼──────────┐
│  M4 匹配订阅   │  │  M6 代投执行    │
│  （发现价值）  │  │  （执行价值）    │            ← 价值层
└───────┬───────┘  └─────┬──────────┘
        │                │
        │          ┌─────▼──────────┐
        │          │  M7 投递记录    │
        │          └─────┬──────────┘
┌───────▼────────────────▼──────────┐
│  M2 归一与增量  ·  M3 用户档案      │            ← 契约层
└───────┬───────────────────────────┘
┌───────▼───────────────────────────┐
│  M1 采集（ATS 适配器）              │            ← 数据源层
└───────────────────────────────────┘
```

### 4.3 模块怎么串起来

发现链是 M1 → M2 → M4 → M5，解决「不错过」。执行链是 M3 → M6 → M7 → M5，解决「不折腾」。两条链在 M5 汇合成一个对话入口，在契约层（M2 的岗位字段加 M3 的档案字段）共享数据。

这里有个可以吃两次的杠杆：国内大中厂的校招官网高度集中在少数几家 ATS 系统上，外企则是另外几家。所以一方面不需要写几百个爬虫，少量通用适配器就能覆盖大头；另一方面投递表单的结构也跟着收敛，采集适配器和填表适配器可以共用同一份公司与系统的识别逻辑。整个方案的成本大头就压在这上面。

### 4.4 模块的 MVP 范围与演进

| 模块 | MVP（7 天） | 后续演进 |
|---|---|---|
| M1 采集 | 3–5 家标杆公司 | ATS 通用适配器 → 数百家 → LLM 兜底长尾 |
| M2 增量 | 简单指纹去重 | 批次识别、跨城市聚合、历年时间线沉淀 |
| M3 档案 | 单用户、本地 | 多档案（不同岗位族用不同版本） |
| M4 匹配 | 规则筛选 | 匹配度打分、优先级排序 |
| M5 交互 | 终端 / 本地输出 | IM 机器人 → 微信 / 小程序 |
| M6 代投 | 2 家，人在环内 | 批量队列、更多 ATS、半自动降级 |
| M7 记录 | 代投即记录 | 邮件回执解析补状态流转 |

### 4.5 哪些将来上云，哪些留在本地

MVP 全部跑在本地，但将来上云不需要重构，因为分界线现在就清楚：

M1 和 M2 将来上云。数据采集是重资产、可共享，也是天然的计费点，API Key 就是鉴权边界。

M3、M6、M7 永远留在本地。简历、身份信息、浏览器登录态不上传，这是隐私底线，同时也能当卖点讲。

M4 和 M5 放哪边都行，届时看体验取舍。

### 4.6 几条底线

1. 登录、注册、验证码、密码，Agent 一律不碰，交回用户人工完成。
2. 提交前必须用户确认，不做静默提交。很多公司限投 1–3 个岗位，投错不可逆。
3. 只采公开的企业招聘信息，不碰任何个人信息。

---

## 五、7 天落地节奏（按功能模块）

### 排期原则

**先定契约，再写实现。** M3 用户档案同时是 M4 筛选的输入和 M6 填表的输入，是产品两半之间的接缝。定晚了前后都要返工，所以必须放在 Day 1。

**发现侧先跑通，执行侧后接上。** 发现侧本身就是一半的产品价值，Day 4 交付的是完整的一半，不是半成品。它排在前面的原因是 M1→M2→M4→M5 属于确定性工程，可以先把地基做稳；M6 属于不确定性工程，而且 Day 1 的人肉走查已经替它探过雷。

**每天收一个可验收的模块，不留半成品跨天。**

> 例外情况：如果 Day 1 走查发现某家公司表单自动化的雷特别大（重验证码、强反自动化、复杂附件流程），立刻把 M6 提到 Day 3，发现侧顺延。不确定的事优先做，确定的事可以往后放。

### 日程

| Day | 主攻模块 | 当天要做的事 | 交付 |
|---|---|---|---|
| 1 | M3 用户档案 + 全局契约 | 选定 3–5 家标杆公司，刻意覆盖 2–3 种不同 ATS；人肉完整走一遍「看官网 → 挑岗位 → 填表 → 提交」；由走查结果反推档案字段集与岗位字段集 | 两份字段约定 + 一份卡点清单 |
| 2 | M1 采集 | 标杆公司的岗位抓取跑通并落本地库 | 能稳定跑的抓取 |
| 3 | M2 归一与增量 | 结构化、指纹去重、「什么算新增」的判定 | 一份可信的每日新增列表 |
| 4 | M4 匹配订阅 + M5 交互调度 | 画像筛选、生成推送文案；封装成 skill/MCP，接上定时任务与自然语言查询 | 发现侧闭环可用 |
| 5 | M6 代投（一） | 打通第一家公司：browser-use 自动填表，填完停住不提交 | 一次成功的自动填表 |
| 6 | M6 代投（二） + M7 投递记录 | 第二家公司验证泛化性；开放题生成；提交前预览确认；投递回写记录 | 执行侧闭环可用 |
| 7 | 集成与验证 | 端到端串测；拉 3–5 个真实同学试用；记录全部失败点 | 一份真实反馈清单 |

### 几个节点

Day 1 收口，契约定死，后面两条线可以并行不打架。
Day 4 收口，已经是一个能用的产品，哪怕代投一行没写。
Day 6 收口，完整闭环，价值主张成立。
Day 7 收口，拿到真人反馈，决定要不要继续投入。

### 最可能出问题的地方

Day 5 到 Day 6。真实 ATS 表单的自动填写，遇到动态渲染、级联下拉、附件上传都可能卡住，这是整个计划里唯一有崩盘风险的环节。

预留一个降级方案：如果全自动打不通，就降到半自动，Agent 预填所有公共字段，把没把握的留空并高亮，用户补最后一成。这个版本的用户价值仍有全自动的七成，足够验证假设。

---

## 六、MVP 验收标准

不达标就说明假设有问题，别急着往下做。两侧分别验收，权重相当。

发现侧：

1. 三家公司的岗位增量连续 3 天无漏报、无重大误报。
2. 试用者确认「原本每天花在巡检上的时间，现在归零了」。
3. 问试用者「有没有你关心但我们没覆盖的公司」，答案是一份可枚举的短名单，而不是「一大堆」。

执行侧：

4. 至少 2 家公司的代投跑通，用户全程只需点确认、填验证码。
5. 从看到推送到完成一次投递，全程 3 分钟以内，对照人工约 20 分钟。

产品验证：

6. 3–5 个真实试用者里，有人主动问「还能加哪些公司」或者「什么时候能用」。

第 6 条最重要，前五条都只是技术验证。第 3 条也值得单独看一眼，它是发现侧唯一能测出覆盖广度够不够的信号，而覆盖广度恰恰是 MVP 阶段最容易自我安慰蒙混过去的指标。

---

## 七、这 7 天不碰的东西

云端部署、API Key 与计费、公司覆盖广度、微信和小程序通道、预测式提醒、多用户隔离、邮件状态解析、简历优化建议、内推撮合、面经生成。

这些方向都对，但都不回答「这个东西到底有没有人要」。

---

## 八、后面的方向（备忘，非本期范围）

**覆盖扩张。** ATS 通用适配器铺到 300 家以上，长尾用结构变更检测加 LLM 抽取兜底。先 diff 后理解，控制成本。

**质量 SLA。** 核心池漏报趋近于零，核心公司开放后告警 2 小时内，假新增率控制在 3% 以下。这是付费用户唯一真正感知的东西。

**商业化。** 按招聘季一次性售卖优于按月订阅。校招消费窗口集中在 7 到 11 月，学生对小额一次性付费的接受度更高。

**季节性对冲。** 接入暑期实习补位淡季，3 到 5 月是高峰。实习生和校招生是同一批人的不同时间点，LTV 可以跨年累积。

**数据资产。** 秋招季持续沉淀历年开放时间线，这是预测式提醒的唯一来源，也是竞品短期抄不走的东西。

**分发下沉。** 插件和 MCP 形态适合技术圈冷启动，但非计算机专业学生自建环境的门槛过高。要扩量必须走 IM 机器人，再到微信通道。
