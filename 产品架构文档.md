# 校招 Agent 产品架构文档

## 一、产品概述

### 1.1 产品定位
校招 Agent 是一个自动化的校招岗位监控与投递系统，专为 26/27 届产品/运营岗求职者设计。

### 1.2 核心价值
- **自动发现**：多源采集，实时监控各大公司校招岗位
- **智能筛选**：基于用户画像的精准匹配推荐
- **自动投递**：浏览器自动化，一键批量投递
- **全程跟踪**：从岗位发布到投递结果的完整追溯

### 1.3 目标用户
- 主要用户：26/27 届应届毕业生
- 目标岗位：产品、运营类非技术岗位
- 目标城市：北上广深杭等一线/新一线城市
- 目标公司：互联网大厂（腾讯、字节、阿里等 20 家）


## 二、系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                          用户层                                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │ CLI 命令  │  │  MCP     │  │  定时    │  │  邮件    │       │
│  │          │  │  Server  │  │  任务    │  │  通知    │       │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘       │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                        应用层（M1-M7）                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │  M1 数据采集  │  │  M4 画像匹配  │  │  M6 自动投递  │         │
│  │  多源适配器  │  │  规则引擎    │  │  浏览器自动化 │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │  M2 归一化   │  │  M5 CLI 交互  │  │  M7 邮件跟踪  │         │
│  │  分类规则    │  │  Rich 输出   │  │  状态更新    │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│  ┌──────────────┐                                              │
│  │  M3 增量检测  │                                              │
│  │  事件驱动    │                                              │
│  └──────────────┘                                              │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                         数据层                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    SQLite 数据库                           │  │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────────┐        │  │
│  │  │ jobs   │ │ events │ │ runs   │ │ submissions │        │  │
│  │  └────────┘ └────────┘ └────────┘ └────────────┘        │  │
│  │  ┌────────┐ ┌────────┐ ┌────────┐                       │  │
│  │  │sources │ │snapshots│ │profile │                       │  │
│  │  └────────┘ └────────┘ └────────┘                       │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                        外部服务层                                │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐      │
│  │ 腾讯招聘  │  │ 字节招聘  │  │ 阿里招聘  │  │ 其他公司  │      │
│  │ API      │  │ API      │  │ API      │  │ (17家)   │      │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘      │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 技术栈

**后端框架**：
- Python 3.13+
- httpx（HTTP 客户端）
- Pydantic（数据验证）
- SQLite（数据库，WAL 模式）

**浏览器自动化**：
- Playwright 1.62.0（Chromium）

**CLI 界面**：
- Typer（命令行框架）
- Rich（格式化输出）

**测试框架**：
- pytest 9.1.1

**包管理**：
- uv（依赖管理和虚拟环境）


## 三、核心模块详解

### 3.1 M1 - 数据采集层

**功能定位**：对接各公司招聘系统，统一数据格式

**架构模式**：适配器模式（Adapter Pattern）

**核心组件**：
```
jobagent/adapters/
├── base.py              # RawJob 数据模型
├── tencent_join.py      # 腾讯 join.qq.com 适配器
├── bytedance.py         # 字节（计划中）
└── alibaba.py           # 阿里（计划中）
```

**数据流**：
```
公司招聘 API
    ↓
Adapter.fetch() → List[RawJob]
    ↓
标准化字段：
- external_id（唯一标识）
- title（岗位名称）
- job_family（职能族）
- cities（城市列表）
- recruit_type（校招/实习）
- grad_year（毕业届别）
- apply_url（投递链接）
```

**已实现**：
- ✅ 腾讯 join.qq.com（795 个岗位）
  - 无鉴权 POST 接口
  - 分页获取
  - 需要 Referer 头

**设计要点**：
1. **容错性**：空响应检测，拒绝将 0 条结果视为正常
2. **审计性**：每次抓取保存原始 JSON 到 snapshots 表
3. **扩展性**：新增公司只需实现 fetch() 方法

---

### 3.2 M2 - 岗位归一化层

**功能定位**：将原始岗位标题映射到统一的职能族分类

**核心逻辑**：三层分类规则（避免误判）

**分类体系**：
```
10 个职能族：
- tech（技术）
- product（产品）
- operations（运营）
- design（设计）
- marketing（市场）
- sales（销售）
- hr（人力）
- finance（财务）
- legal（法务）
- other（其他）
```

**三层规则**：
```python
# 第一层：技术强信号（最高优先级）
TECH_MARKERS = ["大模型", "算法", "Agent", "开发", "研究"]
if any(marker in title for marker in TECH_MARKERS):
    return "tech"

# 第二层：复合职能特例
COMPOUND_RULES = {
    ("员工福利", "薪酬"): "hr",
    ("投资", "风险"): "finance",
}

# 第三层：常规职能词
TITLE_RULES = {
    "运营": "operations",
    "产品": "product",
    "策划": "product",
}
```

**典型案例**：
- ✅ "混元基座模型-视觉理解大模型研究" → `tech`（不是 design）
- ✅ "腾讯营销—广告推荐基础大模型" → `tech`（不是 marketing）
- ✅ "产品运营" → `operations`（不是 product）
- ✅ "运营开发" → `tech`（不是 operations）

**测试覆盖**：31 个测试用例锁定分类规则

---

### 3.3 M3 - 增量检测层

**功能定位**：对比前后两次抓取，产出结构化事件流

**核心机制**：基于指纹的 Diff 检测

**指纹计算**：
```python
fingerprint = hashlib.sha256(
    json.dumps({
        "title": job["title"],
        "cities": job["cities"],
        "job_family": job["job_family"],
        "recruit_type": job["recruit_type"],
        "apply_url": job.get("apply_url"),
    }, sort_keys=True).encode()
).hexdigest()[:16]
```

**事件类型**：
```
1. job_opened     - 岗位首次出现
2. job_reopened   - 岗位重新开放（之前关闭过）
3. job_closed     - 岗位消失/关闭
4. job_updated    - 岗位字段变更（标题/城市/族）
5. family_first_seen - 某公司某族从 0→非0
6. batch_started  - 单日新增突增（疑似批次发布）
7. source_bootstrapped - 首次同步（bootstrap 模式）
```

**安全防护**：
1. **空响应防护**：0 条结果 → 抛异常，拒绝同步
2. **批量关闭防护**：单次消失 ≥40% 且 ≥5 条 → 阻止关闭，记录警告
3. **Bootstrap 模式**：首次同步只产生 1 个 `source_bootstrapped` 事件，避免事件洪水

**事件流存储**：
```sql
CREATE TABLE events (
    id INTEGER PRIMARY KEY,
    kind TEXT NOT NULL,
    source_key TEXT,
    company TEXT,
    job_id INTEGER,
    payload TEXT,  -- JSON: {diff: {field: {old, new}}}
    occurred_at TEXT,
    notified_at TEXT,  -- NULL = 待推送
    run_id INTEGER
);
```

---

### 3.4 M4 - 画像匹配层

**功能定位**：根据用户画像筛选符合条件的岗位

**画像模型**：
```yaml
intent:
  grad_years: ["26", "27"]              # 毕业届别
  families: ["operations", "product"]   # 职能族
  cities: ["北京", "上海", "深圳", "杭州", "广州"]
  recruit_types: ["campus", "intern"]   # 校招/实习
  boost_keywords: ["内容运营", "产品运营", "策略"]
  exclude_keywords: ["外包", "派遣", "客服"]
```

**匹配逻辑**：
```python
def matches(job: dict, intent: dict) -> tuple[bool, int]:
    # 1. 硬性过滤
    if job["grad_year"] not in intent["grad_years"]:
        return False, 0
    if job["job_family"] not in intent["families"]:
        return False, 0
    if not any(city in intent["cities"] for city in job["cities"]):
        return False, 0
    
    # 2. 排除词检查
    if any(kw in job["title"] for kw in intent["exclude_keywords"]):
        return False, 0
    
    # 3. 关键词加分
    score = 0
    for kw in intent["boost_keywords"]:
        if kw in job["title"]:
            score += 10
    
    return True, score
```

**实际效果**（腾讯数据）：
- 总岗位数：795
- 命中画像：42（20 个运营 + 22 个产品）
- 匹配率：5.3%

---

### 3.5 M5 - CLI 交互层

**功能定位**：命令行界面，用户日常操作入口

**命令清单**：
```bash
# 1. init - 初始化数据库
uv run python -m jobagent.cli init

# 2. sync - 同步最新岗位
uv run python -m jobagent.cli sync [--source SOURCE] [--dry-run]

# 3. status - 查看数据源健康度
uv run python -m jobagent.cli status

# 4. jobs - 浏览岗位（支持按族/公司/城市筛选）
uv run python -m jobagent.cli jobs [--family FAMILY] [--company COMPANY]

# 5. digest - 查看增量推送
uv run python -m jobagent.cli digest [--mark]

# 6. apply - 投递单个岗位（M6）
uv run python -m jobagent.cli apply JOB_ID [--profile-path PATH] [--user-data-dir DIR]
```

**输出格式**：Rich 格式化表格
- 彩色输出（绿色=成功，红色=失败，黄色=警告）
- 表格对齐
- 进度提示

**digest 命令核心逻辑**：
```python
# 读取未推送事件（notified_at IS NULL）
events = conn.execute("""
    SELECT * FROM events WHERE notified_at IS NULL
    ORDER BY occurred_at
""").fetchall()

shown = []  # 所有检查过的事件 ID
hits = []   # 命中画像的岗位

for event in events:
    shown.append(event["id"])  # 标记为已检查
    
    if event["kind"] == "job_opened":
        if matches(event, profile):
            hits.append(event)
    elif event["kind"] == "job_updated":
        diff = event["payload"]["diff"]
        if matches(event, profile) and \
           ("job_family" in diff or "cities" in diff):
            changed.append(event)

# 标记已推送
if --mark:
    conn.executemany(
        "UPDATE events SET notified_at=? WHERE id=?",
        [(now(), id) for id in shown]
    )
```


### 3.6 M6 - 自动投递层

**功能定位**：浏览器自动化，模拟人工投递流程

**技术选型**：Playwright（同步 API）

**核心组件**：
```
jobagent/submitters/
├── base.py              # Submitter 协议 + SubmissionResult 模型
└── tencent_join.py      # 腾讯投递器（200 行）
```

**投递流程**：
```
1. 访问岗位页面
   ↓
2. 检测岗位状态（已关闭？）
   ↓
3. 点击「立即申请」
   ↓
4. 检测登录态（需要登录？）
   ↓
5. 填充表单
   - 姓名、手机、邮箱
   - 学校、专业、学历
   - 毕业年份、毕业月份
   - 上传简历（PDF/DOC/DOCX）
   ↓
6. 点击「提交申请」
   ↓
7. 检测结果（成功？重复？）
   ↓
8. 保存截图 + 写入 submissions 表
```

**状态检测**：
```python
class TencentJoinSubmitter:
    def _is_job_closed(self, page: Page) -> bool:
        """检测「已停止招聘」「岗位已下线」等文案"""
        return page.locator("text=/已停止|已下线|已结束/").count() > 0
    
    def _need_login(self, page: Page) -> bool:
        """检测「扫码登录」「手机号登录」等文案"""
        return page.locator("text=/扫码登录|手机号登录/").count() > 0
    
    def _is_success(self, page: Page) -> bool:
        """检测「申请成功」「已提交」等文案"""
        return page.locator("text=/申请成功|提交成功/").count() > 0
    
    def _is_duplicate(self, page: Page) -> bool:
        """检测「已申请」「重复投递」等文案"""
        return page.locator("text=/已申请|重复投递/").count() > 0
```

**登录态持久化**：
```python
# 使用 user_data_dir 保存 cookie/localStorage
ctx = playwright.chromium.launch_persistent_context(
    user_data_dir="~/.cache/playwright-tencent",
    headless=True
)
# 首次手动登录后，后续自动复用
```

**投递记录**：
```sql
CREATE TABLE submissions (
    id INTEGER PRIMARY KEY,
    job_id INTEGER NOT NULL,
    external_id TEXT NOT NULL,
    source_key TEXT NOT NULL,
    company TEXT NOT NULL,
    submitted_at TEXT NOT NULL,
    status TEXT NOT NULL,  -- success, failed, duplicate, closed
    error TEXT,
    screenshot_path TEXT,
    profile_snapshot TEXT,  -- JSON: 提交时的用户画像快照
    created_at TEXT NOT NULL,
    FOREIGN KEY(job_id) REFERENCES jobs(id)
);
```

**使用示例**：
```bash
# 首次投递（手动登录）
uv run python -m jobagent.cli apply 12345 \
  --no-headless \
  --user-data-dir ~/.cache/playwright-tencent

# 后续投递（自动复用登录态）
uv run python -m jobagent.cli apply 67890 \
  --user-data-dir ~/.cache/playwright-tencent
```

**已知限制**：
1. 验证码未处理（缓解：持久化登录态降低触发概率）
2. 反爬风控（建议：每次投递间隔 30-60 秒）
3. 页面改版（维护：定期更新选择器）
4. 多步骤流程（扩展点：自定义逻辑处理问卷/测评）

---

### 3.7 M7 - 邮件跟踪层（计划中）

**功能定位**：解析确认邮件，更新投递状态

**数据源**：
- 投递确认邮件（已投递）
- 简历筛选邮件（已查看、进入下一轮）
- 面试邀请邮件（笔试、面试）
- 拒绝通知邮件（已拒绝）

**状态机**：
```
submitted（已投递）
    ↓
viewed（已查看）
    ↓
screening（简历筛选中）
    ↓
interview_invited（面试邀请）
    ↓
offer（offer）/ rejected（已拒绝）
```

**技术方案**（待定）：
- IMAP 协议读取邮箱
- 正则/NLP 解析邮件内容
- 关键词匹配岗位 ID
- 更新 submissions 表 status 字段


## 四、数据模型

### 4.1 核心表结构

**sources（数据源配置）**
```sql
CREATE TABLE sources (
    source_key TEXT PRIMARY KEY,  -- 唯一标识：tencent_join
    company TEXT NOT NULL,        -- 公司名：腾讯
    system TEXT NOT NULL,         -- 系统类型：self_built/招聘系统名
    entry_url TEXT NOT NULL,      -- 入口地址
    notes TEXT,                   -- 备注
    created_at TEXT NOT NULL
);
```

**jobs（标准化岗位表）**
```sql
CREATE TABLE jobs (
    id INTEGER PRIMARY KEY,
    external_id TEXT NOT NULL,    -- 公司侧岗位 ID
    source_key TEXT NOT NULL,     -- 来源
    company TEXT NOT NULL,        -- 公司
    title TEXT NOT NULL,          -- 岗位标题
    job_family TEXT,              -- 职能族
    raw_category TEXT,            -- 原始分类
    cities TEXT,                  -- JSON 数组
    country TEXT,                 -- 国家
    department TEXT,              -- 部门
    recruit_type TEXT,            -- campus/intern/social
    grad_year TEXT,               -- 毕业届别
    apply_url TEXT,               -- 投递链接
    apply_system TEXT,            -- 投递系统
    description TEXT,             -- 详情
    fingerprint TEXT NOT NULL,    -- 变更检测指纹
    first_seen_at TEXT NOT NULL,  -- 首次发现
    closed_at TEXT,               -- 关闭时间（NULL=开放中）
    raw_json TEXT,                -- 原始 JSON
    UNIQUE(source_key, external_id)
);
```

**events（变更事件流）**
```sql
CREATE TABLE events (
    id INTEGER PRIMARY KEY,
    kind TEXT NOT NULL,           -- 事件类型
    source_key TEXT,
    company TEXT,
    job_id INTEGER,               -- 关联岗位 ID
    payload TEXT,                 -- JSON: {diff: {field: {old, new}}}
    occurred_at TEXT NOT NULL,    -- 事件发生时间
    notified_at TEXT,             -- 推送时间（NULL=待推送）
    run_id INTEGER,               -- 关联批次
    FOREIGN KEY(job_id) REFERENCES jobs(id)
);
```

**submissions（投递记录）**
```sql
CREATE TABLE submissions (
    id INTEGER PRIMARY KEY,
    job_id INTEGER NOT NULL,
    external_id TEXT NOT NULL,
    source_key TEXT NOT NULL,
    company TEXT NOT NULL,
    submitted_at TEXT NOT NULL,
    status TEXT NOT NULL,         -- success/failed/duplicate/closed
    error TEXT,                   -- 失败原因
    screenshot_path TEXT,         -- 截图路径
    profile_snapshot TEXT,        -- JSON: 投递时的用户画像
    created_at TEXT NOT NULL,
    FOREIGN KEY(job_id) REFERENCES jobs(id)
);
```

**snapshots（原始数据审计）**
```sql
CREATE TABLE snapshots (
    id INTEGER PRIMARY KEY,
    source_key TEXT NOT NULL,
    run_id INTEGER NOT NULL,
    raw_json TEXT NOT NULL,       -- 原始 JSON 数组
    created_at TEXT NOT NULL,
    FOREIGN KEY(run_id) REFERENCES runs(id)
);
```

**runs（同步批次）**
```sql
CREATE TABLE runs (
    id INTEGER PRIMARY KEY,
    source_key TEXT NOT NULL,
    started_at TEXT NOT NULL,
    finished_at TEXT,
    status TEXT,                  -- ok/partial/failed
    fetched INTEGER,              -- 抓取数量
    error TEXT,
    FOREIGN KEY(source_key) REFERENCES sources(source_key)
);
```

### 4.2 数据流转

```
公司招聘 API
    ↓
Adapter.fetch() → RawJob[]
    ↓
ingest.sync() 
    ├─ 写入 snapshots（原始 JSON）
    ├─ 计算 fingerprint
    ├─ diff 检测 → events 表
    └─ 更新 jobs 表（status, closed_at）
    ↓
match.matches()
    ├─ 读取 events WHERE notified_at IS NULL
    ├─ 筛选命中画像的事件
    └─ 返回给 CLI digest 命令
    ↓
用户查看 digest 输出
    ↓
CLI digest --mark
    └─ UPDATE events SET notified_at=NOW()
    ↓
用户选择岗位投递
    ↓
CLI apply <job_id>
    ├─ 读取 jobs 表
    ├─ 调用 Submitter.submit()
    └─ 写入 submissions 表
```


## 五、典型使用场景

### 5.1 场景一：每日监控新岗位

**用户故事**：
作为求职者，我希望每天早上看到昨晚新增的匹配岗位，避免错过机会。

**操作流程**：
```bash
# 1. 设置定时任务（每天 9:00）
0 9 * * * cd /path/to/job-agent && uv run python -m jobagent.cli sync

# 2. 手动查看增量
uv run python -m jobagent.cli digest

# 输出示例：
# 新增岗位（命中你的画像）
#   • 产品运营（腾讯）— 北京/上海/深圳
#     https://join.qq.com/post.html?pid=12345
#   • 内容运营（腾讯）— 北京/杭州
#     https://join.qq.com/post.html?pid=67890

# 3. 标记已读
uv run python -m jobagent.cli digest --mark
```

**系统行为**：
1. sync 命令抓取最新数据
2. diff 引擎产出 `job_opened` 事件
3. digest 命令筛选命中画像的岗位
4. --mark 更新 `notified_at`，避免重复推送

---

### 5.2 场景二：批量投递匹配岗位

**用户故事**：
作为求职者，我希望将所有符合条件的岗位一键投递，节省时间。

**操作流程**：
```bash
# 1. 查看所有运营岗位
uv run python -m jobagent.cli jobs --family operations

# 输出示例（表格）：
# 公司  岗位            族    城市          投递链接
# 腾讯  产品运营        运营  北京/上海      https://...?pid=12345
# 腾讯  内容运营        运营  北京/杭州      https://...?pid=67890
# 腾讯  行业运营        运营  深圳          https://...?pid=11111

# 2. 逐个投递（首次手动登录）
uv run python -m jobagent.cli apply 12345 \
  --no-headless \
  --user-data-dir ~/.cache/playwright-tencent
# 浏览器打开 → 扫码登录 → 表单自动填充 → 手动确认提交

# 3. 后续自动投递（复用登录态）
uv run python -m jobagent.cli apply 67890 \
  --user-data-dir ~/.cache/playwright-tencent
uv run python -m jobagent.cli apply 11111 \
  --user-data-dir ~/.cache/playwright-tencent

# 输出示例：
# ✓ 投递成功
#   时间: 2026-08-04T10:30:00
#   截图: screenshots/submit_67890_1722744600.png
```

**系统行为**：
1. jobs 命令展示所有匹配岗位
2. apply 命令启动 Playwright
3. 检测登录态 → 自动填表 → 提交
4. 写入 submissions 表
5. 保存截图审计

---

### 5.3 场景三：岗位变更监控

**用户故事**：
作为求职者，我希望知道之前关注的岗位是否改了城市或要求，决定是否继续投递。

**操作流程**：
```bash
# 1. 同步最新数据
uv run python -m jobagent.cli sync

# 2. 查看变更
uv run python -m jobagent.cli digest

# 输出示例：
# 岗位变更（影响你的画像）
#   • 产品运营（腾讯）— 城市: 北京/上海 → 北京/上海/深圳
#   • 内容运营（腾讯）— 族: 运营 → 产品
```

**系统行为**：
1. sync 时 diff 引擎检测到字段变化
2. 产出 `job_updated` 事件，payload 包含 diff
3. digest 筛选影响画像的变更（城市、族、标题）
4. 过滤纯文案修改（description 变化不产生事件）

---

### 5.4 场景四：投递结果追踪

**用户故事**：
作为求职者，我希望查看已投递岗位的状态，避免重复投递。

**操作流程**：
```bash
# 查询投递历史
sqlite3 data/jobagent.db "
SELECT 
    company, 
    external_id, 
    status, 
    submitted_at, 
    error
FROM submissions
ORDER BY submitted_at DESC
LIMIT 10
"

# 输出示例：
# 腾讯 | 12345 | success   | 2026-08-04T10:30:00 | 
# 腾讯 | 67890 | duplicate | 2026-08-04T10:35:00 | 重复投递
# 腾讯 | 11111 | failed    | 2026-08-04T10:40:00 | 岗位已关闭
```

**系统行为**：
1. 每次 apply 写入 submissions 表
2. status 字段记录结果：success/failed/duplicate/closed
3. error 字段记录失败原因
4. screenshot_path 保存截图路径
5. profile_snapshot 保存投递时的用户画像（避免事后修改导致数据混乱）

---

### 5.5 场景五：多公司并行监控

**用户故事**：
作为求职者，我希望同时监控 20 家公司的岗位，一个地方看到所有更新。

**操作流程**：
```bash
# 1. 同步所有源（未来支持）
uv run python -m jobagent.cli sync --source all

# 2. 查看所有公司状态
uv run python -m jobagent.cli status

# 输出示例（表格）：
# 源            公司    开放岗位  最近同步         状态    抓取数
# tencent_join  腾讯    795      2026-08-04 09:00 ok     795
# bytedance     字节    1200     2026-08-04 09:05 ok     1200
# alibaba       阿里    850      2026-08-04 09:10 ok     850

# 3. 查看所有新增（自动聚合）
uv run python -m jobagent.cli digest

# 输出示例：
# 新增岗位（命中你的画像）
#   • 产品运营（腾讯）— 北京/上海
#   • 内容运营（字节）— 北京/深圳
#   • 用户运营（阿里）— 杭州/上海
```

**系统行为**：
1. 每个公司一个 Adapter
2. sync 命令并发抓取（或串行）
3. 所有事件写入同一个 events 表
4. digest 命令跨公司聚合展示
5. 用户无需关心数据来自哪个公司


## 六、扩展性设计

### 6.1 新增公司适配器

**步骤**：
1. 创建 `jobagent/adapters/<company>.py`
2. 实现 `fetch()` 方法，返回 `List[RawJob]`
3. 在 `cli.py` 注册适配器
4. 编写单元测试验证数据格式

**示例**：字节跳动适配器
```python
# jobagent/adapters/bytedance.py
from .base import RawJob
import httpx

class ByteDanceAdapter:
    source_key = "bytedance"
    company = "字节跳动"
    system = "self_built"
    entry_url = "https://jobs.bytedance.com/"
    
    def fetch(self) -> list[RawJob]:
        resp = httpx.post("https://jobs.bytedance.com/api/search", json={
            "project": "校园招聘",
            "page": 1,
            "limit": 100,
        })
        data = resp.json()
        return [self._to_raw_job(item) for item in data["list"]]
    
    def _to_raw_job(self, item: dict) -> RawJob:
        return RawJob(
            external_id=str(item["id"]),
            title=item["title"],
            raw_json=item,
            job_family=family_from_title(item["title"]),
            cities=item["city"].split(","),
            country="中国",
            recruit_type="campus",
            grad_year="27",
            apply_url=f"https://jobs.bytedance.com/post/{item['id']}",
            apply_system="bytedance",
        )
```

**关键点**：
- `source_key` 必须全局唯一
- `external_id` 必须在同一 source_key 下唯一
- `raw_json` 保存原始数据，便于后续扩展字段
- 容错处理：空响应、超时、异常

---

### 6.2 新增投递器

**步骤**：
1. 创建 `jobagent/submitters/<company>.py`
2. 实现 `Submitter` 协议
3. 在 `cli.py` 注册投递器
4. 编写测试覆盖状态检测逻辑

**示例**：字节跳动投递器
```python
# jobagent/submitters/bytedance.py
from playwright.sync_api import Page
from .base import Submitter, SubmissionResult

class ByteDanceSubmitter:
    source_key = "bytedance"
    company = "字节跳动"
    
    def submit(self, job_id: str, profile: dict) -> SubmissionResult:
        with sync_playwright() as p:
            page = p.chromium.launch().new_page()
            page.goto(f"https://jobs.bytedance.com/post/{job_id}")
            
            # 点击申请按钮
            page.click("text=立即申请")
            
            # 检测登录态
            if self._need_login(page):
                return SubmissionResult(
                    success=False,
                    job_id=job_id,
                    company=self.company,
                    error="需要登录"
                )
            
            # 填表
            self._fill_form(page, profile)
            
            # 提交
            page.click("button:has-text('提交')")
            page.wait_for_timeout(3000)
            
            # 检查结果
            if self._is_success(page):
                return SubmissionResult(
                    success=True,
                    job_id=job_id,
                    company=self.company,
                )
            else:
                return SubmissionResult(
                    success=False,
                    job_id=job_id,
                    company=self.company,
                    error="提交失败"
                )
```

**关键点**：
- 选择器尽量用文本匹配（抗改版）
- 每个关键步骤保存截图
- 返回 `SubmissionResult` 统一格式
- 登录态持久化（`user_data_dir`）

---

### 6.3 MCP Server 集成

**目标**：将 CLI 功能封装为 MCP 工具，集成到 Claude Desktop

**架构**：
```
Claude Desktop
    ↓
MCP Protocol
    ↓
MCP Server（jobagent_mcp.py）
    ↓
复用 jobagent.cli 模块
    ↓
SQLite 数据库
```

**工具清单**：
```python
# mcp_server.py
from mcp.server import MCPServer
from jobagent import cli, db, match

server = MCPServer("jobagent")

@server.tool()
def sync_jobs(source: str = "all") -> dict:
    """同步最新岗位"""
    return cli.sync(source=source, dry_run=False)

@server.tool()
def list_jobs(family: str | None = None) -> list[dict]:
    """列出匹配岗位"""
    conn = db.connect()
    # ... 复用 cli.jobs 逻辑
    return jobs

@server.tool()
def get_digest() -> dict:
    """获取增量推送"""
    conn = db.connect()
    # ... 复用 cli.digest 逻辑
    return {"new": hits, "changed": changed}

@server.tool()
def apply_job(job_id: str, profile_path: str) -> dict:
    """投递岗位"""
    # ... 复用 cli.apply 逻辑
    return result.dict()
```

**优势**：
- Claude 可以自然语言调用工具
- 用户无需记忆命令行参数
- 对话式交互更友好

---

### 6.4 定时任务调度

**目标**：内置调度器，替代 cron

**技术方案**：
- APScheduler（Python 调度库）
- 后台守护进程
- 日志记录每次执行结果

**配置示例**：
```yaml
# schedule.yaml
jobs:
  - name: "每日同步"
    cron: "0 9 * * *"
    command: "sync --source all"
    notify: true
  
  - name: "推送增量"
    cron: "0 9,18 * * *"
    command: "digest"
    notify: true
```

**实现**：
```python
# jobagent/scheduler.py
from apscheduler.schedulers.background import BackgroundScheduler
import yaml

def load_config():
    with open("schedule.yaml") as f:
        return yaml.safe_load(f)

def run():
    scheduler = BackgroundScheduler()
    config = load_config()
    
    for job in config["jobs"]:
        scheduler.add_job(
            func=lambda: cli.run_command(job["command"]),
            trigger="cron",
            **parse_cron(job["cron"])
        )
    
    scheduler.start()
```


## 七、质量保障

### 7.1 测试策略

**测试金字塔**：
```
       ┌─────────────┐
       │  手工测试   │  (5个场景)
       │   M6 投递   │
       └─────────────┘
      ┌───────────────┐
      │  集成测试     │  (1个)
      │  M1→M6 E2E   │
      └───────────────┘
    ┌─────────────────┐
    │   单元测试       │  (49个)
    │ 归一化/增量检测  │
    └─────────────────┘
```

**单元测试覆盖**：
- `test_normalize.py`：31 个测试（岗位分类规则）
- `test_ingest.py`：10 个测试（增量检测逻辑）
- `test_submitter_tencent.py`：8 个测试（投递器）

**集成测试**：
- `test_e2e.py`：端到端验证 M1-M6 协同工作

**手工测试**：
- 真实投递流程（需浏览器交互）
- 验证码处理
- 登录态持久化

**测试数据**：
- Mock 数据：单元测试用
- 生产数据：795 个腾讯岗位验证分类准确性

**测试命令**：
```bash
# 运行所有单元测试
uv run pytest -v

# 运行特定模块测试
uv run pytest tests/test_normalize.py -v

# 运行集成测试
uv run python tests/test_e2e.py

# 查看覆盖率（未配置）
uv run pytest --cov=jobagent tests/
```

---

### 7.2 数据质量

**准确性验证**：
1. **岗位分类**：31 个已知案例锁定分类规则
2. **城市解析**：测试覆盖"中国-北京""北京 上海"等格式
3. **增量检测**：Bootstrap 模式防止事件洪水

**一致性保证**：
1. **外键约束**：`FOREIGN KEY(job_id) REFERENCES jobs(id)`
2. **唯一约束**：`UNIQUE(source_key, external_id)`
3. **非空约束**：关键字段 `NOT NULL`

**审计轨迹**：
1. **snapshots 表**：每次抓取保存原始 JSON
2. **events 表**：所有变更产生事件
3. **submissions 表**：投递历史完整记录
4. **截图**：每次投递保存 4-5 张截图

**数据备份**（建议）：
```bash
# 定期备份 SQLite 数据库
cp data/jobagent.db data/jobagent_backup_$(date +%Y%m%d).db

# 或使用 SQLite 备份命令
sqlite3 data/jobagent.db ".backup data/jobagent_backup.db"
```

---

### 7.3 错误处理

**分层错误处理**：

1. **采集层（M1）**：
   - 空响应检测 → 抛异常，拒绝同步
   - 网络超时 → 重试 3 次
   - API 返回错误 → 记录到 runs.error

2. **增量检测层（M3）**：
   - 批量关闭防护 → 阻止关闭，记录警告
   - 指纹计算失败 → 记录错误，跳过该岗位

3. **投递层（M6）**：
   - 岗位已关闭 → 返回 failed，记录原因
   - 登录态失效 → 返回 failed，提示手动登录
   - 网络超时 → 返回 failed，保存截图
   - 重复投递 → 返回 duplicate

**错误日志**：
```python
# 每个模块记录错误到 runs/submissions 表
conn.execute(
    "UPDATE runs SET status='failed', error=? WHERE id=?",
    (str(exception), run_id)
)
```

**用户提示**：
- 失败时显示清晰的错误消息（中文）
- 提供截图路径便于诊断
- 给出恢复建议（如"手动登录后重试"）

---

### 7.4 性能优化

**数据库优化**：
1. **WAL 模式**：`PRAGMA journal_mode=WAL`（并发读写）
2. **索引**：外键、高频查询字段建索引
3. **分页查询**：大量数据时避免一次加载

**网络优化**：
1. **连接复用**：httpx.Client 复用 HTTP 连接
2. **超时设置**：避免长时间等待
3. **并发抓取**：多源同步可并发（待实现）

**浏览器优化**：
1. **无头模式**：节省资源，提升速度
2. **禁用图片/CSS**：减少加载时间（可选）
3. **登录态持久化**：避免重复登录

**典型性能数据**：
- 单源同步（腾讯 795 岗位）：~10 秒
- digest 命令（查询事件）：< 1 秒
- apply 命令（单次投递）：~15 秒


## 八、部署与运维

### 8.1 部署方式

**本地部署（推荐）**：
```bash
# 1. 克隆项目
git clone <repo_url>
cd job-agent

# 2. 安装依赖
uv sync

# 3. 安装浏览器
uv run playwright install chromium

# 4. 初始化数据库
uv run python -m jobagent.cli init

# 5. 配置用户画像
cp profile.yaml.example profile.yaml
vim profile.yaml

# 6. 首次同步
uv run python -m jobagent.cli sync

# 7. 查看结果
uv run python -m jobagent.cli jobs
```

**服务器部署（可选）**：
```bash
# 1. 使用 systemd 管理定时任务
# /etc/systemd/system/jobagent-sync.service
[Unit]
Description=Job Agent Daily Sync

[Service]
Type=oneshot
User=ubuntu
WorkingDirectory=/home/ubuntu/job-agent
ExecStart=/usr/local/bin/uv run python -m jobagent.cli sync

# /etc/systemd/system/jobagent-sync.timer
[Unit]
Description=Job Agent Daily Sync Timer

[Timer]
OnCalendar=daily
OnCalendar=09:00

[Install]
WantedBy=timers.target

# 启动定时器
systemctl enable jobagent-sync.timer
systemctl start jobagent-sync.timer
```

**Docker 部署（待支持）**：
```dockerfile
FROM python:3.13-slim
WORKDIR /app
COPY . .
RUN pip install uv && uv sync
RUN playwright install --with-deps chromium
CMD ["uv", "run", "python", "-m", "jobagent.cli", "sync"]
```

---

### 8.2 监控指标

**数据源健康度**：
```bash
uv run python -m jobagent.cli status

# 监控指标：
# - 最近同步时间（超过 24 小时报警）
# - 同步状态（failed 报警）
# - 开放岗位数（骤降报警）
```

**投递成功率**：
```sql
SELECT 
    company,
    COUNT(*) as total,
    SUM(CASE WHEN status='success' THEN 1 ELSE 0 END) as success,
    ROUND(100.0 * SUM(CASE WHEN status='success' THEN 1 ELSE 0 END) / COUNT(*), 2) as success_rate
FROM submissions
GROUP BY company;
```

**事件积压**：
```sql
SELECT 
    kind,
    COUNT(*) as count
FROM events
WHERE notified_at IS NULL
GROUP BY kind;

-- 积压超过 100 条报警
```

**数据库大小**：
```bash
ls -lh data/jobagent.db

# 超过 1GB 考虑清理历史数据
```

---

### 8.3 日常维护

**数据清理**（建议每月）：
```sql
-- 清理 3 个月前的 snapshots
DELETE FROM snapshots 
WHERE created_at < date('now', '-3 months');

-- 清理 1 年前的 events
DELETE FROM events 
WHERE occurred_at < date('now', '-1 year');

-- 清理关闭超过 6 个月的 jobs
DELETE FROM jobs 
WHERE closed_at IS NOT NULL 
  AND closed_at < date('now', '-6 months');

-- 真空优化（回收空间）
VACUUM;
```

**日志查看**：
```bash
# 查看最近 10 次同步
sqlite3 data/jobagent.db "
SELECT * FROM runs 
ORDER BY started_at DESC 
LIMIT 10
"

# 查看失败的投递
sqlite3 data/jobagent.db "
SELECT * FROM submissions 
WHERE status='failed' 
ORDER BY created_at DESC 
LIMIT 20
"
```

**备份恢复**：
```bash
# 备份
sqlite3 data/jobagent.db ".backup data/backup_$(date +%Y%m%d).db"

# 恢复
cp data/backup_20260804.db data/jobagent.db
```

**版本升级**：
```bash
# 1. 备份数据库
sqlite3 data/jobagent.db ".backup data/backup_before_upgrade.db"

# 2. 拉取最新代码
git pull

# 3. 更新依赖
uv sync

# 4. 运行数据库迁移（如有）
uv run python -m jobagent.cli migrate

# 5. 验证
uv run pytest -v
```

---

### 8.4 故障排查

**问题：同步失败**
```bash
# 1. 查看错误日志
sqlite3 data/jobagent.db "
SELECT * FROM runs 
WHERE status='failed' 
ORDER BY started_at DESC 
LIMIT 1
"

# 2. 检查网络连接
curl -I https://join.qq.com/

# 3. 测试适配器
uv run python -c "
from jobagent.adapters.tencent_join import TencentJoinAdapter
adapter = TencentJoinAdapter()
jobs = adapter.fetch()
print(f'抓取到 {len(jobs)} 个岗位')
"
```

**问题：投递失败**
```bash
# 1. 查看截图
ls -lt screenshots/ | head -5
open screenshots/submit_<job_id>_*.png

# 2. 查看失败原因
sqlite3 data/jobagent.db "
SELECT error FROM submissions 
WHERE job_id='<job_id>'
"

# 3. 手动测试（有头模式）
uv run python -m jobagent.cli apply <job_id> \
  --no-headless \
  --user-data-dir ~/.cache/playwright-tencent
```

**问题：digest 没有新增**
```bash
# 1. 检查是否是首次同步（bootstrap 模式）
sqlite3 data/jobagent.db "
SELECT * FROM events 
WHERE kind='source_bootstrapped'
"
# 如果有记录，再运行一次 sync 即可

# 2. 检查 notified_at 是否已标记
sqlite3 data/jobagent.db "
SELECT COUNT(*) FROM events 
WHERE notified_at IS NULL
"
# 如果为 0，说明已全部标记
```

**问题：数据库锁定**
```bash
# WAL 模式下极少发生，如遇到：
# 1. 关闭所有连接
pkill -f jobagent

# 2. 手动检查点
sqlite3 data/jobagent.db "PRAGMA wal_checkpoint(FULL);"

# 3. 重启
uv run python -m jobagent.cli status
```


## 九、发展路线图

### 9.1 当前状态（M1-M6 已完成）

| 里程碑 | 状态 | 完成度 | 交付内容 |
|-------|------|--------|---------|
| M1 数据采集 | ✅ | 100% | 腾讯适配器（795 岗位） |
| M2 岗位归一化 | ✅ | 100% | 10 个职能族，三层分类规则 |
| M3 增量检测 | ✅ | 100% | 事件驱动，安全防护 |
| M4 画像匹配 | ✅ | 100% | 规则引擎 + 关键词打分 |
| M5 CLI 交互 | ✅ | 100% | 6 个命令，Rich 输出 |
| M6 自动投递 | ✅ | 100% | Playwright + 腾讯投递器 |

**核心指标**：
- 代码量：~3000 行（含测试和文档）
- 测试覆盖：49 个单元测试 + 1 个集成测试
- 支持公司：1 家（腾讯）
- 岗位覆盖：795 个（腾讯校招/实习）
- 投递成功率：待生产验证

---

### 9.2 短期计划（1-2 周）

**优先级 P0**：
1. **批量投递命令**
   - `apply-batch` 命令
   - 读取 digest 输出，批量投递
   - 间隔控制（30-60 秒）
   - 失败重试机制

2. **投递统计 CLI**
   - `submissions` 命令
   - 查看投递历史和成功率
   - 按公司/状态筛选

3. **网络重试机制**
   - 指数退避（1s, 2s, 4s）
   - 最多重试 3 次
   - 记录重试次数

**优先级 P1**：
4. **配置文件优化**
   - 支持多个 profile（工作/实习）
   - 支持环境变量（敏感信息）

5. **日志系统**
   - structlog 结构化日志
   - 分级别输出（DEBUG/INFO/ERROR）
   - 日志轮转

---

### 9.3 中期计划（1 个月）

**优先级 P0**：
1. **M7 邮件跟踪**
   - IMAP 协议读取邮箱
   - 解析确认/筛选/面试/拒绝邮件
   - 更新 submissions 表状态
   - 新增状态机字段

2. **验证码处理**
   - 集成打码平台（2captcha）
   - 或人工介入机制（暂停等待）
   - 降级策略（跳过该岗位）

3. **5 家公司适配器**
   - 字节跳动（jobs.bytedance.com）
   - 阿里巴巴（campus.alibaba.com）
   - 华为（career.huawei.com）
   - 美团（zhaopin.meituan.com）
   - 京东（zhaopin.jd.com）

**优先级 P1**：
4. **投递器扩展**
   - 为上述 5 家公司实现 Submitter
   - 统一选择器策略
   - 共享登录态管理逻辑

5. **数据分析面板**
   - 岗位趋势图（新增/关闭）
   - 投递漏斗（投递→查看→面试→offer）
   - 公司对比（岗位数、匹配率）

---

### 9.4 长期计划（2-3 个月）

**优先级 P0**：
1. **MCP Server 封装**
   - 实现 MCP 协议
   - 封装 6 个核心工具
   - 发布到 Claude Desktop

2. **定时任务调度器**
   - 内置 APScheduler
   - 替代 cron
   - 支持 YAML 配置

3. **20 家公司全覆盖**
   - 完成所有目标公司适配器
   - 统一数据源管理
   - 健康度监控

**优先级 P1**：
4. **Web 界面（可选）**
   - FastAPI 后端
   - React 前端
   - 岗位卡片流
   - 可视化投递状态

5. **移动端推送（可选）**
   - 企业微信/钉钉机器人
   - 邮件通知
   - Telegram Bot

6. **AI 增强（可选）**
   - 岗位描述摘要（LLM）
   - 简历匹配度打分
   - 自动生成求职信

---

### 9.5 技术债务清理

**代码质量**：
- [ ] 添加类型注解（mypy 检查）
- [ ] 增加测试覆盖率至 80%+
- [ ] 代码规范检查（ruff）
- [ ] 文档字符串完善

**架构优化**：
- [ ] 引入依赖注入（避免硬编码 DB 路径）
- [ ] 抽象数据库层（支持 PostgreSQL）
- [ ] 插件化适配器注册
- [ ] 配置热加载

**性能优化**：
- [ ] 并发抓取多个数据源
- [ ] 数据库查询优化（EXPLAIN ANALYZE）
- [ ] 大数据分页处理
- [ ] Redis 缓存热点数据

**安全增强**：
- [ ] 敏感信息加密存储（简历路径、邮箱）
- [ ] API Key 管理
- [ ] 数据备份自动化
- [ ] 审计日志


## 十、附录

### 10.1 技术决策记录

**为什么选择 SQLite 而不是 PostgreSQL？**
- ✅ 零配置，开箱即用
- ✅ 单文件存储，易于备份
- ✅ WAL 模式支持并发读写
- ✅ 对于单用户场景足够
- ❌ 不支持多服务器横向扩展（暂不需要）

**为什么选择 Playwright 而不是 Selenium？**
- ✅ 更现代的 API（async/await）
- ✅ 内置智能等待机制
- ✅ 跨浏览器支持（Chromium/Firefox/WebKit）
- ✅ 网络拦截能力（可扩展为 API mock）
- ❌ 社区生态不如 Selenium（可接受）

**为什么不使用 ORM（如 SQLAlchemy）？**
- ✅ 直接 SQL 更灵活、更透明
- ✅ 减少依赖，降低复杂度
- ✅ 性能更优（无 ORM 开销）
- ❌ 需要手写 SQL（schema 简单，可接受）

**为什么使用 Typer + Rich 而不是 Click？**
- ✅ Typer 基于类型提示，代码更简洁
- ✅ Rich 提供开箱即用的彩色输出和表格
- ✅ 文档和错误提示友好
- ❌ Click 生态更成熟（Typer 已足够）

**为什么使用 uv 而不是 pip/poetry？**
- ✅ 速度快（Rust 实现）
- ✅ 依赖解析准确
- ✅ 虚拟环境管理简单
- ✅ 兼容 pip 生态
- ❌ 相对新，社区较小（发展趋势好）

---

### 10.2 目录结构

```
job-agent/
├── jobagent/                    # 核心模块
│   ├── __init__.py
│   ├── cli.py                   # M5 CLI 入口
│   ├── db.py                    # 数据库工具
│   ├── ingest.py                # M3 增量检测引擎
│   ├── match.py                 # M4 画像匹配
│   ├── normalize.py             # M2 归一化规则
│   ├── schema.sql               # 主表结构
│   ├── schema_submissions.sql   # M6 扩展表
│   ├── adapters/                # M1 数据采集适配器
│   │   ├── __init__.py
│   │   ├── base.py              # RawJob 模型
│   │   └── tencent_join.py      # 腾讯适配器
│   └── submitters/              # M6 投递适配器
│       ├── __init__.py
│       ├── base.py              # Submitter 协议
│       └── tencent_join.py      # 腾讯投递器
├── tests/                       # 测试
│   ├── test_ingest.py           # 增量检测测试
│   ├── test_normalize.py        # 归一化测试
│   ├── test_submitter_tencent.py # 投递器测试
│   └── test_e2e.py              # 端到端测试
├── docs/                        # 文档
│   ├── M6_MANUAL_TEST.md        # 手工测试指南
│   ├── M6_SUMMARY.md            # M6 实现总结
│   └── 语言环境检查报告.md      # 语言统一性报告
├── scripts/                     # 脚本
│   └── demo_submit.py           # 演示脚本
├── data/                        # 数据目录
│   └── jobagent.db              # SQLite 数据库
├── screenshots/                 # 投递截图
├── profile.yaml                 # 用户画像
├── profile.yaml.example         # 配置示例
├── pyproject.toml               # 项目配置
├── README.md                    # 项目主文档
├── QUICK_REF.md                 # 快速参考
├── M6_COMPLETE.md               # M6 完成报告
└── 产品架构文档.md               # 本文档
```

---

### 10.3 关键文件说明

**核心代码**：
- `cli.py`：命令行入口，定义 6 个子命令
- `ingest.py`：增量检测核心逻辑，diff 算法实现
- `normalize.py`：三层分类规则，岗位族映射
- `match.py`：画像匹配规则引擎
- `db.py`：数据库连接、初始化、工具函数

**适配器**：
- `adapters/base.py`：RawJob 数据模型定义
- `adapters/tencent_join.py`：腾讯招聘 API 对接

**投递器**：
- `submitters/base.py`：Submitter 协议、SubmissionResult 模型
- `submitters/tencent_join.py`：Playwright 投递实现

**配置**：
- `profile.yaml`：用户画像（筛选条件 + 个人信息）
- `schema.sql`：数据库表结构
- `pyproject.toml`：依赖管理

**文档**：
- `README.md`：项目介绍、快速开始、架构设计
- `QUICK_REF.md`：快速参考手册
- `M6_COMPLETE.md`：M6 完成报告
- `docs/M6_MANUAL_TEST.md`：手工测试指南

---

### 10.4 常见问题（FAQ）

**Q1：支持哪些公司？**
A：当前仅支持腾讯（join.qq.com），计划扩展至字节、阿里等 20 家。

**Q2：如何添加新公司？**
A：参考 `jobagent/adapters/tencent_join.py`，实现 `fetch()` 方法即可。

**Q3：投递失败怎么办？**
A：查看截图（`screenshots/` 目录）和错误信息，常见原因是登录态失效或岗位关闭。

**Q4：如何避免重复投递？**
A：系统会自动检测重复投递，返回 `duplicate` 状态。

**Q5：数据存储在哪里？**
A：SQLite 数据库存储在 `data/jobagent.db`，可随时备份。

**Q6：支持多人使用吗？**
A：当前设计为单用户本地使用，多人场景需部署 PostgreSQL + Web 界面。

**Q7：如何设置定时同步？**
A：使用 cron（Linux/Mac）或 Task Scheduler（Windows）定时执行 `sync` 命令。

**Q8：投递后如何追踪状态？**
A：查看 `submissions` 表记录，未来 M7 将自动解析邮件更新状态。

**Q9：是否支持社招岗位？**
A：支持，但需在 `profile.yaml` 中添加 `social` 到 `recruit_types`。

**Q10：如何清理历史数据？**
A：参考"8.3 日常维护"中的 SQL 清理脚本。

---

### 10.5 贡献指南

**开发环境搭建**：
```bash
git clone <repo_url>
cd job-agent
uv sync
uv run pytest -v
```

**提交规范**：
- feat: 新功能
- fix: 修复 bug
- docs: 文档更新
- test: 测试用例
- refactor: 重构

**测试要求**：
- 新增功能必须有单元测试
- 所有测试必须通过
- 代码覆盖率不低于当前水平

**代码审查**：
- 遵循 PEP 8 规范
- 使用类型提示
- 中文注释和文档字符串
- 提交前运行 `ruff check`

---

### 10.6 联系方式

**项目地址**：<repo_url>

**问题反馈**：GitHub Issues

**技术支持**：<support_email>

**协议**：MIT License

---

## 总结

校招 Agent 是一个完整的端到端校招岗位监控与投递系统，覆盖从数据采集、智能筛选到自动投递的全流程。

**核心优势**：
1. **自动化程度高**：从发现到投递全自动
2. **扩展性好**：适配器模式易于新增公司
3. **数据完整**：完整的审计轨迹和历史记录
4. **用户友好**：中文界面、清晰的错误提示
5. **质量保障**：49 个单元测试 + 集成测试

**当前限制**：
1. 仅支持 1 家公司（腾讯）
2. 验证码未处理
3. 邮件跟踪未实现
4. 缺少 Web 界面

**未来方向**：
1. 扩展至 20 家公司
2. 实现 M7 邮件跟踪
3. 发布 MCP Server
4. 构建数据分析面板

校招 Agent 为求职者提供了一个强大的自动化工具，大幅降低了校招投递的时间成本，提升了求职效率。

---

**文档版本**：v1.0  
**最后更新**：2026-08-04  
**作者**：Job Agent Team

