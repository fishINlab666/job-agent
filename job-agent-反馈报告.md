# job-agent 实现评审反馈

> 评审对象：https://github.com/fishINlab666/job-agent
> 范围：实现框架与技术细节。全量阅读源码（约 3500 行），本地跑通测试套件（320 passed，与 SPEC.md 声明一致），两处疑点写了复现脚本验证。
> 日期：2026-08-06

## 一、实现框架

先说做得对的。按"招聘系统 × 租户"组织适配器、路由证据分级（域名命中才可路由，页面标记只当线索）、租户配置和链接证据对不上时拒绝启动，这一层抽象经得起推敲，加一家用同款 SaaS 的公司只需要配置一行，成本结构撑得住往几十家铺。两阶段投递闸门是全仓库设计最扎实的部分：投递被拆成填表、人工确认、提交三步，中间用一次性令牌和字段摘要校验锁死，防重放、防过期、防页面漂移都有测试锁定，还有一条专门的守门测试防止将来有人图省事把一步到底的 submit() 加回来。

框架层面有两个问题。

### 1. 事务边界散落在各个写入点，这类 bug 会反复出现（已复现一例）

现在是一个数据库连接贯穿整轮多源同步，每个写函数自带 commit 开关，靠调用方记得传对。db.py 和 ingest.py 里那些解释 dry-run 泄漏的大段注释说明这个雷已经踩过至少三次，而同类的第四个还在，我写脚本复现了：

同一批抓取里出现重复的 external_id（翻页期间岗位池在变动时会发生），插入撞唯一约束抛 IntegrityError。ingest.sync 的 try 只包了 fetch()，没包写库循环，异常一路抛到 CLI；CLI 捕获后 continue 处理下一个源，而下一个源的第一次 commit 会把失败源留在事务里的半截数据一起提交进库。复现结果：残留了半批 jobs 和 snapshots，run 状态永远停在 running。

根治办法不是再加一个 commit 开关，而是把事务边界收到一处：ingest.sync() 拥有整个事务，入口开始、成功提交、任何异常回滚，把散在 register_source、start_run、finish_run 里的 commit 参数全部去掉（start_run 需要先落盘留崩溃痕迹的话，用独立连接单写那一行）。这一改能把 CLAUDE.md 里自己称为"头号 bug 来源"的整个类别关掉。

### 2. 三份文档口径打架，其中一份会把协作者带进沟里

产品架构文档（v1.0）还在描述一步到底的 submit() 协议，它的"新增投递器示例"就是填完表直接点提交，还在引用已经废弃的 submissions 表和"一键批量投递"的定位。这三样全部和现在的代码相反：submit() 被测试明确禁止，submissions 已迁移合并，apply 命令有意不提供跳过确认的开关。README 也是旧的（测试数写 41，实际 320；投递流程描述里没有人工确认这一步）。三份文档里只有 SPEC.md 和代码一致。

按现状，一个照着产品架构文档上手的协作者，第一个 PR 就会和安全闸门对着干。建议产品架构文档删掉或顶部标注已废弃，README 的 M6 一节重写。

## 二、技术问题清单

按严重程度排序，前两条已用脚本复现。

| # | 问题 | 位置 | 严重度 |
|---|------|------|--------|
| 1 | 城市变更永远不会推送给用户 | ingest.py:199 / cli.py:215 | 高 |
| 2 | 同批重复 id 导致半截数据混入库 | ingest.py:175 / cli.py:70 | 高 |
| 3 | 腾讯 recruit_type 兜底违反自家规则 | adapters/tencent_join.py:47 | 高 |
| 4 | 腾讯分页中断静默截断 | adapters/tencent_join.py:100 | 中 |
| 5 | 届别窗口换季后存量数据不更新 | ingest.py:36 | 中 |
| 6 | sync all 一个源坏掉中止全部 | cli.py:66 | 中 |
| 7 | 两处行为与文档/自身不一致 | cli.py:372 / submitters/tencent_join.py:155 | 低 |

**1. 城市变更不推送。** 岗位变更事件的 diff 字段清单里没有 cities。城市变化会因为指纹变动触发 job_updated 事件，但事件负载里的 diff 是空的，于是 digest 里专门渲染"城市: 北京 → 深圳"的那段代码是永远走不到的死代码。复现确认：改城市后 diff keys 为空。对按城市硬过滤的用户来说，岗位从北京改成只剩深圳约等于关闭，这恰恰是文档场景三承诺要推送的变更。修的时候注意库里存的 cities 是 JSON 字符串、新数据是 list，直接比较永远不相等，这大概就是当初把它排除出 diff 的原因，需要先反序列化再比。

**2. 重复 id 引发的事务污染。** 机制见框架部分第 1 条。触发条件是 offset 翻页期间源站岗位池发生增删，属于低概率但迟早发生的场景。短期缓解可以在适配器 fetch 里按 external_id 去重，根治靠事务边界收拢。

**3. 腾讯适配器的招聘类型兜底。** 认不出的标签一律归为校招（campus），还连带把届别写成 26。这直接违反 SPEC §6 的硬规则"认不出就写 None，不许兜底"——飞书适配器为同一件事专门修过 bug 并写了回归测试，腾讯这边同一函数零测试覆盖。源站哪天加一种新的招聘项目标签，一批岗位会被静默标成确定命中的 26 届校招，比判不出糟糕得多。

**4. 腾讯分页静默截断。** 接口说总数还有、批次却返回空时，腾讯适配器带着半截数据正常返回；飞书适配器对完全相同的情况是抛异常拒绝返回。40% 的关闭守卫能兜住大截断，但 30% 的截断会穿过去，把两百多条在招岗位误判关闭。把飞书那三行逻辑抄过来即可。

**5. 届别窗口换季失效。** 腾讯的届别是按招聘项目推导的常量（应届→26、实习→27），注释里写了每个招聘季要重新核对。问题是 grad_year 不在指纹字段里：就算换季时记得改常量，存量岗位因为指纹没变走的是"只更新最后见到时间"的分支，库里的届别停在旧值，永远不会更新。要么把届别加进指纹，要么换季时手动批量更新。

**6. 一个源坏掉拖垮整轮同步。** sync all 循环里，路由失败走的是直接退出进程，而三行之下抓取失败走的是 continue。sources 表里一行配置写错（比如 system 拼错），后面所有健康的源那天全都不同步。对一个把"漏报开放窗口"定义为最大损失的产品，这个处理是反的，也应该 continue。

**7. 两处不一致，可接受但建议明确。** 一是 apply --dry-run 会往真库写一条投递记录，而 SPEC §7 写的是"dry-run 全程不落盘"（那条硬约束原本是针对 sync 的，留痕可能是有意的，建议在文档里把两种 dry-run 的语义分开写清）。二是 execute() 对无效令牌是抛异常，对过期、已用、漂移三种情况是返回 blocked 结果，单进程 CLI 触发不了这条路径，但将来包成 MCP 服务时它就是一个未捕获异常。

## 三、修改建议的优先顺序

1. **事务边界收拢到 ingest.sync()。** 一次重构关掉一整类 bug，其余写入点全部简化。
2. **修问题 1、3、4。** 三个都是小改动，都有现成的对照实现或测试模式可以抄。
3. **问题 5、6 顺手修掉。** 各自几行的改动。
4. **文档收敛。** 产品架构文档标废弃，README 重写投递一节，避免误导协作者。
