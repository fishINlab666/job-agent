# 个人信息，绝对不提交
profile.yaml
# 备份/临时副本也算个人信息。只写 profile.yaml 一行挡不住 profile.yaml.bak
# —— 编辑器和手改配置常留这些后缀，而这个文件里是真实姓名手机号。
profile.yaml.*
!profile.yaml.example
*.bak
*.orig
*~

# 本地数据
data/
*.db
*.db-wal
*.db-shm

# 浏览器登录态（M6 用，含 cookie）
.browser-profile/

__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.DS_Store
